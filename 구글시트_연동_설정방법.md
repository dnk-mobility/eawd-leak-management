# 마스터 샘플 트렌드 — 구글시트 연동 설정 방법

> 작업자마다 **폰이 달라도 같은 데이터**를 보게 만드는 설정입니다.
> 한 번만 하면 되고, 약 10~15분 걸립니다. 프로그램 설치나 새 서비스 가입은 없습니다.

---

## 먼저 알아 두실 것

| 질문 | 답 |
|---|---|
| 새로 가입해야 하나요? | **아니요.** 이미 쓰시는 구글(gmail) 계정 하나면 됩니다. |
| 작업자도 계정이 필요한가요? | **아니요.** 작업자는 지금처럼 QR 찍고 입력만 합니다. 로그인 화면이 뜨지 않습니다. |
| 돈이 드나요? | 아니요. 구글 스프레드시트·Apps Script 모두 무료 범위 안입니다. |
| 사이트 주소가 바뀌나요? | 아니요. `dongnam959.github.io` 그대로입니다. |
| 설정 안 하면 어떻게 되나요? | 지금처럼 **입력한 폰에만** 기록이 남습니다. 페이지는 정상 동작합니다. |

**설정하는 사람 = 구글 계정 주인 한 명**입니다. 이 사람 계정의 스프레드시트에 데이터가 쌓입니다.

---

## 1단계 — 스프레드시트 만들기

1. 브라우저에서 <https://sheets.new> 접속 (구글 계정 로그인 상태여야 함)
2. 빈 스프레드시트가 열리면 왼쪽 위 **"제목 없는 스프레드시트"** 를 눌러 이름을 바꿉니다
   → 예: `마스터샘플 누설값 기록`

> 시트 탭(`기록`, `마스터샘플`)은 **직접 만들 필요 없습니다.** 첫 저장 때 자동으로 생깁니다.

---

## 2단계 — Apps Script에 코드 붙여넣기

1. 상단 메뉴 **[확장 프로그램] → [Apps Script]** 클릭 → 새 탭이 열립니다
2. 편집기 가운데에 아래처럼 기본 코드가 들어 있습니다. **전부 선택해서 지웁니다.**
   ```
   function myFunction() {
   }
   ```
3. 저장소의 **`tools/apps-script/Code.gs`** 파일을 메모장 등으로 열어 **내용 전체를 복사**해서 붙여넣습니다
4. 왼쪽 위 **저장(💾) 아이콘** 클릭
5. 왼쪽 위 프로젝트 이름(`제목 없는 프로젝트`)도 알아보기 쉽게 바꿔 둡니다 → 예: `누설값 저장 서버`

### (선택) 암호 문구 바꾸기

코드 윗부분의 이 줄이 일종의 암호입니다. 기본값 그대로 써도 되지만 바꾸려면:

```javascript
var TOKEN = 'dnk-leak-2026';
```

바꿨다면 **반드시** 저장소의 `leak-sync.js` 안 같은 줄도 똑같이 바꿔야 합니다. 두 값이 다르면 "토큰이 올바르지 않습니다" 오류가 납니다.

---

## 3단계 — 웹 앱으로 배포하기

1. 오른쪽 위 파란 **[배포]** 버튼 → **[새 배포]**
2. 왼쪽 **톱니바퀴(⚙) 아이콘** → **[웹 앱]** 선택
3. 아래처럼 설정합니다 — **세 번째 항목이 제일 중요합니다**

   | 항목 | 설정값 |
   |---|---|
   | 설명 | `v1` (아무거나) |
   | 다음 사용자로 실행 | **나 (본인 계정)** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** ← 반드시 이것 |

   > "모든 사용자"로 해야 작업자 폰이 **로그인 없이** 저장할 수 있습니다.
   > "나만"이나 "조직 내 사용자"로 두면 작업자 폰에서 연결 실패가 납니다.

4. **[배포]** 클릭

### 처음 한 번만 — 권한 승인

배포를 누르면 "액세스 승인" 창이 뜹니다. 구글이 처음 보는 스크립트라 경고를 보여 주는 것이며, 본인이 방금 만든 코드이므로 진행하면 됩니다.

1. **[액세스 승인]** → 본인 구글 계정 선택
2. "Google에서 확인하지 않은 앱" 경고 화면 → 왼쪽 아래 **[고급]** 클릭
3. 맨 아래 **["(프로젝트 이름)"(으)로 이동(안전하지 않음)]** 클릭
4. **[허용]** 클릭

### 주소 복사

배포가 끝나면 **웹 앱 URL** 이 나옵니다. 이런 모양입니다:

```
https://script.google.com/macros/s/AKfycbx................../exec
```

**[복사]** 를 눌러 두세요. 이 주소가 저장소 역할을 합니다. (창을 닫았으면 **[배포] → [배포 관리]** 에서 다시 볼 수 있습니다.)

---

## 4단계 — 폰 한 대에서 먼저 시험

1. 폰(또는 PC 브라우저)에서 트렌드 페이지를 엽니다
   → `https://dongnam959.github.io/eawd-leak-management/?eq=120`
2. 접속 암호 입력 후 맨 아래 **⑤ 데이터 · 연동 설정** 을 펼칩니다
3. **연동 주소** 칸에 3단계에서 복사한 주소를 붙여넣습니다
4. **[연결 시험]** 클릭 → `연결 성공 — 서버 시각 ...` 이 뜨면 정상
5. **[주소 저장]** 클릭
6. 화면 위쪽 띠가 **초록 점 + "구글시트 연동됨"** 으로 바뀌는지 확인
7. 시험 삼아 값을 하나 저장해 보고, 1단계에서 만든 스프레드시트에 **`기록` 시트가 생기고 줄이 추가되는지** 확인합니다

> 이 단계에서 저장한 주소는 **그 폰에만** 적용됩니다. 다음 단계를 해야 모든 폰에 적용됩니다.

---

## 5단계 — 모든 폰에 적용 (사이트에 반영)

1. 저장소의 **`leak-sync.js`** 파일을 엽니다
2. 위쪽의 이 줄을 찾습니다

   ```javascript
   var URL = "";
   ```

3. 따옴표 안에 3단계에서 복사한 주소를 넣습니다

   ```javascript
   var URL = "https://script.google.com/macros/s/AKfycbx.................../exec";
   ```

4. 저장하고 push 합니다

   ```bash
   git add leak-sync.js && git commit -m "구글시트 연동 주소 설정" && git push origin main
   ```

5. 몇 분 뒤 **다른 폰**으로 QR을 찍어 들어가서, 4단계에서 넣은 값이 **그대로 보이는지** 확인합니다
   → 보이면 설정 완료입니다.

---

## 평소 사용 방법 (작업자)

지금까지와 똑같습니다. 달라지는 건 **화면 위쪽 띠**뿐입니다.

| 표시 | 뜻 | 할 일 |
|---|---|---|
| 🟢 구글시트 연동됨 | 정상 | 그냥 입력하면 됩니다 |
| 🔵 전송 중… / 불러오는 중… | 통신 중 | 잠깐 기다립니다 |
| 🟡 미전송 N건 | 전파가 약해 아직 못 보냄 (**입력은 이미 폰에 저장됨**) | 통신 되는 곳에서 **[새로고침]** 한 번 |
| 🔴 연동 오류 | 주소·설정 문제 | 관리자에게 알림 |

- **전파가 안 터져도 입력은 됩니다.** 폰에 먼저 저장하고, 통신이 되면 자동으로 보냅니다.
- 다른 작업자가 넣은 값을 보려면 화면을 새로 열거나 **[새로고침]** 을 누르면 됩니다.

---

## 코드를 고쳤을 때 — 가장 흔한 실수

Apps Script는 **코드를 저장해도 배포본에는 자동 반영되지 않습니다.** 반드시 아래를 해야 합니다.

1. **[배포] → [배포 관리]**
2. 기존 배포 오른쪽 **연필(✏) 아이콘** 클릭
3. **버전**을 **[새 버전]** 으로 변경
4. **[배포]** 클릭

> **[새 배포]를 누르면 주소가 새로 생깁니다.** 그러면 `leak-sync.js` 의 주소도 다시 바꿔야 하므로, 반드시 위처럼 **[배포 관리] → 버전만 올리기**를 하세요.

---

## 문제 해결

| 증상 | 원인 / 조치 |
|---|---|
| `연결 실패 — 주소가 맞는지...` | 주소가 `/exec` 로 끝나는지 확인. `/dev` 로 끝나면 안 됩니다 (그건 본인만 접근 가능한 시험용 주소) |
| `토큰이 올바르지 않습니다` | `Code.gs` 의 `TOKEN` 과 `leak-sync.js` 의 `TOKEN` 이 다릅니다. 같게 맞추고 **재배포** |
| 내 폰은 되는데 다른 폰은 안 됨 | 배포 설정의 **"액세스 권한이 있는 사용자"** 가 "모든 사용자"인지 확인 |
| 주소를 넣었는데 다른 폰은 그대로 | 5단계(`leak-sync.js` 에 주소 넣고 push)를 안 했습니다. ⑤에서 넣은 주소는 그 폰에만 적용됩니다 |
| 코드를 고쳤는데 안 바뀜 | 위 "코드를 고쳤을 때" 참고 — 배포 버전을 올려야 합니다 |
| 시트에 일자가 숫자로 보임 | 해당 열을 선택 → [서식] → [숫자] → [일반 텍스트] |
| `응답 시간 초과` | 통신 상태 문제. [새로고침] 다시 시도. 입력값은 폰에 남아 있으므로 없어지지 않습니다 |

---

## 알아 둘 한계

- **보안 수준은 사이트 접속 암호와 같은 정도입니다.** 배포 주소와 TOKEN을 아는 사람은 기록을 읽고 쓸 수 있습니다. 실제 보안 장치가 아니라 "아무나 우연히 건드리지 못하게" 하는 수준입니다 (`PROJECT_OVERVIEW.md` §8과 같은 성격).
- **누가 입력했는지는 "확인자" 칸에 적은 이름으로만 구분됩니다.** 계정 로그인이 없으므로 시스템이 사람을 식별하지는 못합니다.
- **같은 일자·교대를 두 사람이 각각 저장하면 나중에 저장한 값이 남습니다.** (일자+교대 1건 = 기록 1건 구조)
- 구글 Apps Script는 개인 계정 기준 하루 실행 횟수 제한이 있지만(2만 회 수준), 이 용도는 하루 수십 회라 문제되지 않습니다.
- 스프레드시트는 **원본 기록 보관용**입니다. 시트에서 직접 값을 고쳐도 페이지에 반영되지만, 열 순서·시트 이름을 바꾸면 동작하지 않습니다.
