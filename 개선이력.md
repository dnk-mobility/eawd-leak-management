# e-AWD 모터 하우징 누설값 관리 시스템 — 개선 이력 (상세)

> 후공정 생산기술팀 · 시스템 구축·점검 원본 기록
> 이 문서는 개선 항목별로 **문제 → 발견 경위 → 원인 → 위험도 → 조치 → 검증 → 재발방지**를 남기는 누적 기록입니다.
> 시스템 구조 전반은 [`PROJECT_OVERVIEW.md`](PROJECT_OVERVIEW.md) 참고. 같은 형식의 자매 시스템 기록은 QR 정보관리 시스템의 `개선이력.md`.
> **새 항목은 파일 맨 위에 추가합니다** (최신이 먼저 보이도록).

---

## 2026-09-21 (2차) 섹션 색깔 구분 · 그래프 디테일 보강(면적 채우기·가로축 눈금·요일)

대시보드 디자인안 검토 중 함께 받은 요청 3건. 대시보드 자체는 아직 화면안 검토 단계라 구현하지 않았고(별도 Artifact로 시안만 다듬음), 아래 3건은 실서비스 코드에 반영했다.

### [1] ①~⑥ 섹션이 전부 같은 색이라 구분이 안 됨 — 위험도: 낮음 (사용성)

**요청.** "월별기록 등 탭을 구분되게 해줘" → "색깔로 말이야"로 구체화됨 — 섹션(①시업 전 점검 입력 ~ ⑥데이터·연동 설정)이 전부 같은 남색 번호 배지·같은 흰 배경이라, 스크롤하다 보면 지금 몇 번 섹션을 보고 있는지 한눈에 안 들어온다는 지적.

**조치.** 섹션마다 고유 색을 하나씩 배정해 번호 배지 배경 + 카드 위쪽 3px 띠에 적용했다: ①남색(`#1b4a49`) ②틸(`#2f6e6c`, 그래프 누설값 선과 동일 색) ③앰버(`#a8752f`, 추세선과 동일 색) ④플럼(`#7d5b8f`) ⑤러스트(`#9c5a3c`) ⑥슬레이트(`#4a6670`). `styles.css`는 QR 정보관리 시스템과 공유하는 파일이라 거기서 고치지 않고, `index.html`에서 섹션 id별로 덮어쓰는 방식을 썼다.

**검증.** 6개 섹션 각각의 `border-top` 색과 `.num` 배지 배경색이 의도한 값으로 다르게 렌더링되는 것을 확인.

### [2] 누설값 트렌드 그래프 — 주간 보기에 요일이 없음 — 위험도: 낮음 (기능 추가)

**조치.** 주간 보기(`period === "week"`)일 때만 X축 라벨에 요일을 더했다 — "21" → "21(월)". `weekdayKo()`를 `dateFromStr()`(기존에 쓰던, 타임존 문제를 피하려고 `"T00:00:00"`을 붙여 로컬 자정으로 파싱하는 헬퍼) 기반으로 새로 만들어, 이미 있는 날짜 파싱 방식을 그대로 재사용했다. 월간 보기는 점이 많아 요일까지 넣으면 라벨이 빽빽해지므로 그대로 뒀다.

**검증.** 주간 보기에서 그래프 X축 라벨이 "21(월)"처럼 표시되는 것을 확인.

### [3] 그래프가 "성의없어 보인다" — 면적 채우기·가로축 세부 눈금 추가 — 위험도: 낮음 (시각 보강)

**요청.** 대시보드 시안의 미니 그래프에 대한 피드백("디테일을 살려줘, 성의없어 보인다")과 함께, 실제 앱의 ②누설값 트렌드 그래프에도 같은 방향으로 디테일을 더해 달라는 요청. 가로축도 "미세하게" 나타내 달라는 요청도 함께 받음.

**조치.**
1. 꺾은선 아래에 옅은 그라디언트 면적 채우기를 추가했다(게이트 화면의 장식 그래프와 같은 방식) — 값만 찍힌 선이 아니라 "채워진 그래프"처럼 보이게 한다.
2. X축 기준선 위에 데이터 점마다 짧은 눈금(ruler tick)을 찍어, 축만 보고도 기록 횟수가 드러나도록 했다.

**구현 중 짚어 둘 점 — SVG id 충돌 위험.** 면적 채우기는 `<linearGradient id="...">`를 SVG마다 새로 정의해야 하는데, `buildChart()`는 한 화면에서 샘플마다(OK 마스터·NG 마스터 등) 여러 번 호출된다. 모든 `<svg>`가 같은 `id="chartFill"`을 그대로 쓰면 **문서 전체에서 첫 번째 정의만 유효해**(중복 id는 HTML/SVG에서 무효) 다른 샘플의 채우기가 서로 뒤섞일 수 있었다. `"chartFill-" + s.id`(마스터 샘플 고유 id)로 그라디언트 id를 만들어 막았다.

**검증.** 월간 보기에서 그래프 카드 2장(OK 마스터·NG 마스터) 모두에 면적 채우기가 각각 별도의 `linearGradient` id로 정상 렌더링되는 것과, X축에 데이터 점 수만큼 세부 눈금이 찍히는 것을 확인.

**재발방지.** 한 페이지 안에서 같은 그리기 함수가 여러 번 호출되어 여러 개의 `<svg>`를 만드는 구조에서, 그 안에 `id`가 필요한 요소(그라디언트, 클립패스, 마스크 등)를 새로 추가할 때는 **항상 호출마다 고유한 id를 만들 것** — 값이 우연히 같아 보여도(이번처럼 색이 다 똑같아서 겉보기엔 문제가 안 드러남) 나중에 요소별로 다른 값을 쓰게 되면 그제서야 겉으로 드러나는 잠복 버그가 된다.

---

## 2026-09-21 게이트 문구 등장 시간 재조정 (사소한 조정)

8차에서 그래프와 같이 나오도록 문구 등장 시점을 앞당기고 지속 시간도 늘렸는데, "조금만 더 길게" 재요청을 받아 브랜드·설비명·밑줄의 등장 애니메이션 길이를 한 단계 더 늘렸다(1.6/1.8/1s → 2.0/2.2/1.3s). 등장 시점(delay)은 그대로 둬 그래프와 같이 나오는 느낌은 유지된다. `animationend` 개수 기반 종료 로직은 delay/duration 값과 무관하게 동작하므로 이 값만 바꾸면 되고 다른 코드는 손댈 필요 없었다.

---

## 2026-09-21 그래프 카드 안에서 주간 이동 불가 — 위험도: 낮음 (배치 개선)

**문제.** 그래프를 주간 보기로 바꾼 뒤 다른 주로 이동할 방법이 그래프 카드 안에는 없다는 보고.

**원인분석.** 주/월 이동 버튼(‹ 라벨 ›, `#prevM`/`#nextM`)은 처음부터 페이지 맨 위(①시업 전 점검 입력보다도 위, `.month-bar`)에만 있었다. 로직 자체(`shiftPeriod()`)는 이미 주간 모드에서도 정상 동작했지만, ②그래프까지 스크롤한 사용자 입장에서는 그 버튼이 화면 밖에 있어 "이동할 방법이 없다"는 인상을 준다 — 8차의 주간/월간 토글 버튼과 정확히 같은 종류의 문제([7차][5] 참고)를, 이번엔 토글이 아니라 이동 버튼에서 놓쳤다.

**조치.** ②그래프 카드 안(주간/월간 토글 바로 아래)에 같은 이동 바를 하나 더 뒀다(`#monthBar2`, `#prevM2`/`#nextM2`/`#mLabel2`/`#mMeta2`). `renderMonth()`가 라벨·안내문·다음 버튼 비활성 여부를 페이지 맨 위 것과 항상 함께 갱신한다.

**검증.** 그래프 카드 안에서 "‹"를 누르면 `#mLabel2`와 페이지 맨 위 `#mLabel`이 동시에 "9/21~9/27" → "9/14~9/20"으로 바뀌는 것을 확인.

**재발방지.** 페이지 상단에만 있는 컨트롤이 특정 섹션의 상태를 바꾸는 구조에서는, **그 컨트롤이 하는 일이 "표시 방식 전환"이든 "이동/탐색"이든 종류에 상관없이** 그 섹션까지 스크롤한 사용자 눈에 보이는 자리에도 있어야 한다 — 토글 하나를 이 패턴으로 고쳤다고 해서 같은 화면의 다른 컨트롤(이동 버튼)까지 저절로 해결되는 것은 아니라는 점을 이번에 놓쳤다.

---

## 2026-09-19 (8차) 게이트 디테일 강화 · 매뉴얼 기반 챗봇 보강 · 주간/월간 회귀분석 추가

디텍터(LS-R700) 조작·설치 매뉴얼 2건과 LS-R902 매뉴얼(네트워크 공유폴더), `70kW 모터 하우징 리크 MSA.xlsx`를 참고 자료로 받아 반영했다.

### [1] 게이트 그래프 — 숫자·점선·실선 더 진하게, 문구 타이밍 변경 — 위험도: 낮음 (요청 반영)

**조치.**
1. 그래프 전체 밝기(`.bg-line`의 `opacity`)를 `.55` → `.75`로 올리고, 점선 기준선(`.22`→`.45`, 굵기 1→1.2), 실선 꺾은선(굵기 2→2.6), y축 눈금 숫자·단위 라벨(`opacity` `.45~.55`→`.7~.85`, `font-weight:700` 추가)을 각각 더 진하게 만들었다.
2. 브랜드·설비명 문구가 그래프와 **같이** 나오도록 등장 시점을 앞당겼다(전에는 그래프가 다 그려진 2초 뒤에야 문구가 시작됨 → 이제 꺾은선이 그려지기 시작하는 시점(0.5~0.6초)에 맞춤). 문구가 화면에 나타나는 데 걸리는 시간(등장 애니메이션 길이)도 늘렸다(브랜드 1.1s→1.6s, 설비명 1.2s→1.8s, 밑줄 0.7s→1s) — "그래프 등장할 때 같이 위에 글도 나오게 하고, 그 등장 타임을 조금 더 길게" 요청.

**검증.** 브라우저에서 로드 직후(약 1.3초 경과 시점) 문구와 그래프가 동시에 옅게 나타나기 시작하는 것, 이후 완전히 그려진 상태에서 숫자·점선·실선이 이전보다 뚜렷하게 보이는 것을 스크린샷으로 확인.

### [2] 챗봇 — 매뉴얼 기반 실질적인 답변 보강 — 위험도: 낮음 (콘텐츠 보강)

**문제.** "검색해도 효과가 없는 것 같다"는 지적과 함께, 이 앱이 실제로 쓰는 통계 용어("표준편차")를 검색해도 아무것도 안 뜬다는 구체적 사례를 받음. 리크 디텍터(LS-R700/R902) 매뉴얼 3건을 참고 자료로 받음.

**원인분석.** "표준편차"는 ②그래프의 추이 이상 신호가 **실행 중에 동적으로 생성하는 문장 안에만** 등장했다 — FAQ 배열(`q`/`keywords`/`a`)의 정적 텍스트 어디에도 이 단어가 없어, 검색 대상 자체가 아니었다. FAQ 콘텐츠 자체가 "이 화면 사용법"에 치우쳐 있고, 디텍터의 실제 동작·문제 원인 같은 더 실질적인 내용이 부족했다.

**조치.** 매뉴얼에서 실제 근거를 찾아 FAQ 3건을 추가했다.
- **"표준편차가 뭔가요?"** — 이 앱의 추이 이상 신호가 왜 이 수치를 쓰는지 설명.
- **"마스터 NG가 반복해서 뜨면 어떻게 하나요?"** — LS-R700 조작 매뉴얼 "9 문제해결 > 6 Master NG 다발 시"의 확인 순서(No-Leak Test → Work측 압력 상승 원인(씰·온도) → Master측 압력 하강 원인(마스터·배관 누설, 배관재 변형, 단열압축) → 과잉보정 의심(보정값 갱신))를 그대로 옮겼다.
- **"보정값(Comp Value)이 뭔가요?"** — 조작 매뉴얼 "6.5 Fixed Comp Settings"의 "Fixed Comp Value(보정값)" 설명을 이 앱의 ⑤디텍터 보정 이력 기능과 연결했다.

**매뉴얼 확보 관련 메모.** 이 프로젝트가 참고한 2개 PDF는 `pdftotext`로 뽑으면 한글 부분이 대부분 빈 칸으로 나왔다(임베디드 폰트의 ToUnicode 매핑 문제로 추정) — PyMuPDF(`pymupdf`, Python)로 다시 뽑으니 정상적으로 추출됐다. **앞으로 이런 PDF에서 한글 텍스트가 깨지거나 비면, `pdftotext` 대신 PyMuPDF(`page.get_text()`)를 먼저 시도할 것.**

**검증.** 챗봇에 "표준편차", "마스터 NG 반복", "comp value"로 각각 검색해 새로 추가한 항목이 결과에 포함되는 것을 확인.

**재발방지.** 도움말 콘텐츠를 "이 화면은 이렇게 씁니다" 수준에만 머물게 하지 말 것 — 사용자가 실제로 겪는 현장 문제(설비 이상, 반복 NG 등)에 대한 답은 설비 매뉴얼 등 1차 자료에서 가져와야 신뢰할 수 있다. 새 FAQ를 추가할 때 이미 화면 안에서 쓰는 용어(이번엔 "표준편차")가 있다면, 그 용어 자체를 질문으로도 검색되게 해 둘 것.

### [3] 그래프 — 주간·월간 "분석"이 항상 똑같았음 — 위험도: 낮음 (기능 추가)

**문제.** 주간 보기와 월간 보기를 바꿔도 그래프 카드의 분석 내용이 똑같다는 지적. (6차에서 추이 이상 신호를 "전체 기록 기준"으로 바꾼 것이 원인 — 통계적으로는 맞는 선택이었지만, 그 결과 카드 안에 "지금 보이는 기간에 따라 달라지는" 분석이 하나도 안 남게 됐다.) `70kW 모터 하우징 리크 MSA.xlsx`(Type 1 Gage Study)를 참고 자료로 받아, 현장에서 평균·표준편차·기울기 같은 통계를 실제로 쓰고 있음을 확인했고, 추세선·기울기·R²을 추가해 달라는 요청을 받음.

**조치.** 그래프 카드에 **그 카드에 지금 그려진 points(주간이면 그 주, 월간이면 그 달)만으로 계산한** 선형회귀 추세선을 추가했다(`linreg()`, 최소제곱법). 그래프에는 주황색 점선으로 추세선을 그리고, 그 밑에 "추세선(회귀분석): 측정 1회당 X mL/min · R² Y(직선에 가까운 정도: 약함/보통/뚜렷함)"을 표시한다(`regressionStatsHtml()`). 점이 2개뿐이면 회귀가 항상 완벽하게 들어맞아 의미가 없으므로 3개 이상일 때만 그린다. **추이 이상 신호(전체 기록 기준)와 성격이 다르다는 것을 카드 안 주석·문서에 명시**해 둔다 — 추세선은 "지금 보이는 기간의 흐름", 추이 이상 신호는 "이 샘플 역사 전체와 비교한 이상 여부"로 역할이 나뉜다.

**설계 판단 — x축을 무엇으로 삼았나.** 날짜 간격이 아니라 **그래프에 찍히는 순서**(0, 1, 2 …)를 x로 썼다 — 그래프의 X() 함수 자체가 이미 순서 기준으로 점을 배치하고 있어(날짜 간격 비례가 아님), 추세선도 같은 기준으로 맞춰야 그래프 위에 그려지는 직선이 어긋나지 않는다. 그래서 기울기 단위는 "측정 1회당"이지 "하루당"이 아니다(교대별로 하루 1~2회 기록될 수 있어 서로 다르다).

**검증.** 같은 샘플을 월간 보기(기울기 +0.076, R² 0.176)와 주간 보기(기울기 +0.71, R² 0.408)로 각각 확인해 서로 다른 값이 나오는 것을 확인. 그래프 SVG 안에 주황색(`#a8752f`) 점선 추세선이 실제로 그려지는 것도 확인.

**재발방지.** "전체 기록 기준으로 바꾸면 더 정확해진다"는 개선이, 그 대가로 "지금 보이는 기간에 대한 정보"를 하나도 안 남기는 부작용을 만들 수 있다(6차 항목이 그랬다) — 통계 계산 범위를 바꿀 때는 **그 계산이 원래 담당하던 다른 역할(이번 경우 "이 기간엔 이런 흐름이었다"는 감각)까지 같이 없어지는 건 아닌지 확인할 것.** 둘 다 필요하면 이번처럼 역할을 나눠 별도로 두는 편이 낫다.

---

## 2026-09-19 (7차) 게이트 2단계 구조 재설계 · 챗봇 유사질문 추천 · 주간 분석 공백 · 보정이력 잠금 권한 조정

### [1] 게이트가 QR 정보관리 시스템과 "개념이 다르다" — 위험도: 낮음 (구조 재설계)

**문제.** QR 정보관리 시스템은 QR을 찍을 때마다 ① 브랜드·제목 화면(효과 포함) → ② 별도의 암호 화면, 이렇게 **화면이 한 번 바뀌는 2단계** 구조다. 이 프로젝트는 배경 애니메이션 위에 카드 하나만 있고, 그 카드 안에 브랜드·제목·(필요하면) 암호 입력칸이 처음부터 같이 들어있어 사실상 1단계였다.

**원인분석.** QR 시스템은 `intro.js`(브랜드·제목, 입력칸 없음)와 `gate.js`(순수 암호 카드)가 **완전히 분리된 두 개의 파일·두 개의 오버레이**로, 앞 단계가 끝나야(Promise) 뒤 단계가 시작한다. 이 프로젝트는 처음부터 `gate.js` 한 파일 안에 배경 연출과 암호 카드를 한 화면으로 합쳐서 설계했다 — 두 시스템이 "같은 걸 만들었다"고 생각했지만 구조 자체가 달랐다.

**조치.** `gate.js`를 진짜 2단계로 다시 짰다. **1단계(항상 재생)** = 격자·스캔·꺾은선 그래프 + 브랜드·설비명·밑줄, 입력칸 없음(QR의 intro.js 역할). 이 장면이 끝나면 이미 인증된 탭은 **2단계 없이** 바로 콘텐츠로 넘어가고, 아직이면 1단계 전용 요소(스캔선·그래프·문구)만 빠르게 사라지며 **2단계** = 순수 암호 카드가 새로 나타난다(QR의 `intro.js → gate.js ask()` 전환과 같은 구조). 타이밍도 QR의 `인트로_암호게이트_구현상세.md`와 같은 값(TAIL_MS 1000ms · FADE_MS 1200ms)으로 맞췄다 — 이전 값(900/700)은 QR보다 빨라 "느낌이 다르다"는 인상의 일부였을 것으로 본다. 암호가 맞았을 때도 이제는 즉시 넘어가지 않고 FADE_MS만큼 페이드아웃한 뒤 넘어간다(예전엔 즉시 `reveal()` — 이미 통과한 탭의 마감 방식과 달라 어색했다).

**구현 중 자체 발견 — 심각한 회귀(다중 카드 중첩).** 리팩터링 과정에서 기존 "이미 통과한 탭" 흐름에 있던 `closed` 가드(한 번 실행되면 다시 실행되지 않게 막는 플래그)를 실수로 빠뜨렸다. `animationend` 카운터가 0 밑으로 내려간 뒤에도 이벤트가 더 들어오면 `setTimeout(finishPhase1, TAIL_MS)`가 매번 새로 걸려 `finishPhase1()`이 여러 번 실행되고, 2단계에서는 **암호 카드가 여러 장 겹쳐 쌓이는** 눈에 띄는 버그로 나타났다(브라우저로 직접 재현해 카드 6장이 겹쳐 있는 것을 확인). `phase1Done` 플래그를 다시 추가해 "한 번만 실행"을 보장하도록 고쳤다.

**검증.** 브라우저에서 ① 새 탭(비인증) 진입 시 1단계(브랜드·설비명·그래프)가 뜬 뒤 2단계(암호 카드)로 정상 전환되는 것, ② 카드가 정확히 1장만 존재하는 것(`document.querySelectorAll('.dnk-gate-card').length === 1`), ③ 올바른 암호 입력 시 `sessionStorage`에 값이 설정되고 게이트가 제거되는 것, ④ 이미 인증된 탭에서 새로고침하면 암호 카드 없이 1단계 뒤 바로 콘텐츠로 넘어가는 것(`hasCard:false`)을 각각 확인했다.

**재발방지.** 애니메이션 완료를 "이벤트 개수 세기"로 판정하는 코드를 리팩터링할 때는, **"한 번만 실행" 가드가 기존에 있었다면 반드시 그대로 유지할 것** — 이벤트 카운터 방식은 카운트가 실제와 아주 조금만 어긋나도(음수로 내려가도) 조용히 여러 번 실행되는 함정이 있고, 이 프로젝트에서 이미 한 번(5차, `animated` 목록 누락) 겪은 것과 같은 종류의 문제가 가드 누락이라는 다른 경로로 또 발생했다. 리팩터링 시 "이 안전장치가 왜 있었는지"부터 확인하고 옮길 것.

### [2] 게이트 문구·그래프 디테일 보강 — 위험도: 낮음 (요청 반영 + 오탈자 수정)

**문제.** 게이트 문구가 "e-AWD 모터 하우징"으로 **"70kW"가 빠져** 있었고, "누설값 관리 시스템"을 "마스터 누설값 관리 시스템"으로 바꿔 달라는 요청. 또한 배경 꺾은선 그래프가 "더 전문적이고 멋있게" 보이도록(실제 그래프처럼 숫자·점 추가) 개선해 달라는 요청.

**조치.** 문구를 "e-AWD 70kW 모터 하우징 / 마스터 누설값 관리 시스템"으로 수정(위 [1]의 1단계 텍스트). 그래프는 지점을 6개→10개로 늘리고, y축 눈금 숫자 3개("6"·"3"·"0")와 단위 라벨("mL/min")을 옅게 추가해 실측 그래프에 더 가까운 인상을 주도록 했다(장식용 — 실제 값과 무관).

**검증.** 브라우저 스크린샷으로 문구·그래프(지점 10개, 눈금 숫자, 단위 라벨)가 의도대로 렌더링되는 것을 확인.

### [3] 챗봇 — 일치하는 게 없으면 그냥 "못 찾았습니다" — 위험도: 낮음 (기능 개선)

**문제.** 검색어가 FAQ 문구와 겹치는 단어가 하나도 없으면(오타, 낯선 표현 등) 바로 "일치하는 답을 못 찾았습니다"만 뜨고 끝 — 사용자 입장에서 챗봇이 "입력해도 반응이 없다"는 인상을 준다는 지적.

**조치.** 정확히 일치하는 게 없을 때는 2글자 단위(bigram) 유사도로 "그나마 비슷한" FAQ 질문을 최대 4개까지 찾아 "정확히 일치하는 답은 못 찾았지만, 혹시 이런 게 궁금하신가요?"로 추천한다(`fuzzySearchFaq()`).

**구현 중 자체 발견 — 유사도 계산식이 FAQ의 keywords 길이에 불리했다.** 처음에는 `hits / Math.max(검색어 bigram 수, 대상 bigram 수)`로 점수를 매겼는데, FAQ 항목의 `keywords`가 길면(예: "샘플 없음 등록 추가") 대상 쪽 길이가 늘어나 분모가 커지면서, 검색어와 핵심 부분이 거의 그대로 겹치는 질문도 점수가 낮게 나와 걸러지는 문제가 있었다(오타를 넣은 "마스터샘풀이없다고나옴"으로 재현 — 계산상 일치 비율이 높은데도 0건). **검색어 쪽 기준으로**(`hits / 검색어 bigram 수`) 계산하도록 고쳐, "내가 입력한 것 중 몇 %가 이 질문에서 발견되는가"를 보게 했다. 임계값도 0.12 → 0.3으로 올려 관련 없는 질문이 섞이지 않게 했다.

**검증.** "마스터샘풀이없다고나옴"(오타) → "마스터 샘플이 없다고 나와요" 추천, "xyzqwerty12345랜덤문자열"(전혀 무관) → 추천 없이 기존 "못 찾았습니다" 문구 유지, 두 경우 모두 브라우저에서 확인.

**재발방지.** 문자열 유사도를 "겹치는 개수 ÷ 무언가의 길이"로 정규화할 때는, **그 "무언가"가 사용자 입력과 무관하게 늘어날 수 있는 값(여기서는 FAQ 작성자가 붙인 keywords)이면 점수가 왜곡될 수 있다는 것을 미리 의심할 것** — 정규화 기준은 대체로 "내가 통제할 수 있는 쪽"(검색어)으로 잡는 편이 직관과 맞는다.

### [4] 주간 보기에서는 추이 이상 신호가 사실상 안 뜸 — 위험도: 낮음 (통계적 한계, 코드로 보완)

**문제.** 그래프를 "주간"으로 보면 ⚠추이 이상 신호 패널이 안 뜬다는 보고.

**원인분석.** `detectTrend()`의 SPC 규칙들은 최소 6~10개의 데이터가 있어야 판정이 성립한다(연속추세 6개·평균쏠림 8개·경고구간근접 8개·변동성급증 10개). 그런데 이 값은 **지금 보이는 기간(week=최대 7일치)의 점들만** 넘겨 계산하고 있었다 — 하루 1회 점검이면 한 주 데이터가 딱 6~7개뿐이라, 사실상 항상 "신호 없음"이 나올 수밖에 없는 구조였다. 코드 결함이라기보다, 애초에 "지금 보이는 기간만 분석"이라는 설계 자체가 통계적으로 무의미한 경우였다.

**조치.** 그래프 카드의 추이 분석은 지금 보이는 기간(주간/월간)과 무관하게 **그 샘플의 전체 기록**(`sampleHistory(s)` — 챗봇의 실시간 분석이 쓰는 것과 동일)으로 계산하도록 바꿨다. 그래프 자체(점·선)는 그대로 "지금 보이는 기간"만 그린다 — 분석만 전체 기록 기준으로 바뀐다. 이 때문에 분석 결과에 인용된 날짜가 지금 화면의 기간 밖일 수 있어, 안내 문구에 그 점을 명시했다.

**검증.** 주간 보기로 전환한 상태에서 그래프 카드마다 `.trend-note`가 정상적으로 렌더링되는 것을 확인(이전이라면 데이터 부족으로 비어 있었을 상황).

**재발방지.** 통계적 판정에 최소 표본 수가 필요한 기능을 "화면에 보이는 기간"에 묶어 두면, 보기 단위(이번 경우 주간/월간 전환, §4-3차)가 늘어날 때 조용히 무력화될 수 있다 — **판정 로직에 넘기는 데이터와 "화면에 그리는" 데이터를 같은 소스로 묶어 두지 말 것.**

### [5] 화면 맨 아래에서 맨 위로 돌아갈 방법이 없음 — 위험도: 낮음 (기능 추가)

**조치.** 화면을 400px 이상 내리면 우측 하단에 "맨 위로" 버튼이 나타난다(`#toTopBtn`). 챗봇 버튼(`#helpFab`)과 겹치지 않도록 그 바로 위에 쌓았다. 누르면 `scrollTo({top:0, behavior:'smooth'})`로 맨 위로 이동한다(동작 줄이기 환경에서는 `behavior:'auto'`로 즉시 이동).

**검증.** 스크롤 위치가 400px를 넘으면 버튼에 `.show` 클래스가 붙는 것, 클릭 시 `window.scrollTo`가 `{top:0, behavior:'smooth'}` 인자로 정확히 호출되는 것을 확인. (참고: 이 자동화 환경에서는 `behavior:'smooth'` 스크롤 애니메이션 자체가 실제로 진행되지 않아 스크롤 위치 변화까지는 확인하지 못했다 — 호출 자체는 맞게 이뤄지므로 실제 브라우저에서는 정상 동작할 것으로 판단.)

### [6] 「기타사항」으로 잠그는 기능이 관리자 모드 밖에 있었음 — 위험도: 낮음 (권한 범위 조정)

**경위.** 원래(3차)는 "잠그기는 누구나, 풀기는 접속 암호, 입력·삭제만 관리자 암호"로 설계했다 — 이력 표 조회와 마찬가지로 잠그기도 "외부인에게 안 보이게" 하는 수준의 기능이라 굳이 관리자 권한을 요구할 필요가 없다고 판단했었다. 이번에 "관리자 모드로 전환해서 들어가면 입력·수정과 「기타사항」 잠그기가 같이 있게 해 달라"는 명시적 요청을 받아, 잠그기도 관리자 모드 안으로 옮겼다.

**조치.** 「기타사항」으로 잠그기 버튼(`calibLockBtn`)과 안내문을 `calibLockZone`으로 묶어, 관리자 모드가 풀렸을 때만(`calibEditor`와 동시에) 보이도록 `renderCalibGate()`를 수정했다. 이제 **잠그기도 관리자 암호(`dnkm8721*`)가 있어야 가능**하다 — 잠금 해제(접속 암호)는 이전과 동일하게 그대로 둔다.

**검증.** 관리자 모드 전환 전에는 `calibLockZone.hidden === true`(버튼 자체가 없음), 관리자 암호로 전환한 뒤에는 `calibLockZone.hidden === false`로 버튼이 나타나는 것을 확인.

**재발방지.** "누구나 할 수 있어도 되는 기능"과 "권한이 있어야 하는 기능"의 경계는 시스템을 처음 설계할 때의 판단이 시간이 지나면서 바뀔 수 있다 — 특히 이번처럼 "같은 화면 안에서 여러 기능이 서로 다른 잠금 수준을 갖는" 구조에서는, 요청이 오면 **주저 없이 범위를 다시 그릴 것**(이미 적용된 권한 경계를 "원래 그렇게 설계했으니까"로 방어하지 않기).

---

## 2026-09-19 (6차) NG 점 상시 강조 · 게이트 문구·연출 조정 · 챗봇 "먹통" 원인 · 주간/월간 재배치

5차 배포 직후 받은 후속 피드백 6건. [1]·[5]는 5차에서 만든 기능을 요청대로 조정한 것, [4]는 5차 자체가 새로 만들어 낸 회귀에 가깝다.

### [1] NG 점 강조를 "마우스를 올려야만"이 아니라 항상 보이게 — 위험도: 낮음 (요청에 따른 설계 변경)

**경위.** 5차에서 PC 전용 `:hover` 펄스 효과로 구현했는데(터치 기기의 sticky-hover 부작용을 피하려는 의도), "마우스가 안 가도 나오게 해달라"는 요청을 받음 — 처음부터 눈에 띄어야 한다는 취지.

**조치.** `@media (hover:hover) and (pointer:fine)` 가드와 `:hover` 선택자를 없애고, `.pt-ng`에 애니메이션을 상시로 걸었다. PC·폰 구분이 없어지므로 sticky-hover 문제 자체가 발생할 여지도 함께 사라졌다(더 이상 hover 상태에 의존하지 않기 때문). `prefers-reduced-motion`에서는 그대로 끈다.

**검증.** NG 판정 점의 `getComputedStyle().animationName`이 `"ngPulse"`, `animationIterationCount`가 `"infinite"`로 항상 걸려 있음을 확인(마우스를 올리지 않은 상태에서 확인).

### [2] 게이트 안내 문구 — 지속 방식을 화면에 설명하지 않기 — 위험도: 낮음 (보안 문구 조정)

**문제.** "이 화면을 닫지 않는 동안에는 다시 묻지 않습니다 / QR을 다시 찍거나 새로 열면 한 번 더 확인합니다"라는 문구가, 뒤집어 보면 "탭을 안 닫으면 인증이 계속 유지된다"는 재인증 회피 방법을 그대로 알려주는 셈이라 악용 소지가 있다는 지적.

**조치.** 문구를 "계속하려면 접속 암호를 입력해 주세요."로 바꿔 지속 방식에 대한 설명을 뺐다. **실제 동작(sessionStorage)은 그대로**다 — 화면에 그 사실을 설명하지 않을 뿐이다.

**재발방지.** 보안·인증 관련 안내 문구를 쓸 때는 "사용자에게 편의를 알려주는 것"과 "우회 방법을 알려주는 것"이 한 문장 안에서 같은 말일 수 있다는 점을 항상 의식할 것 — 특히 지속 시간·재확인 주기처럼 "언제 안 물어보는지"를 구체적으로 설명하는 문구는 재검토 대상이다.

### [3] 게이트 애니메이션 — 그래프 디테일 강화, 격자 진하게 — 위험도: 낮음 (요청에 따른 시각 보강)

**조치.**

1. 격자 선 색을 배경과 가까운 톤(`#2c5f5d`)에서 더 밝은 톤(`#3d7a78`)으로 올리고, 기존 촘촘한 보조선(28px 간격) 위에 굵은 주 눈금(112px 간격, `#4a8f8c`)을 한 겹 더 겹쳐 "계측 그래프"에 가까운 인상을 줬다.
2. 꺾은선 그래프에 디테일 3가지를 더했다 — ① 옅은 점선 기준선 1개, ② 선 아래 그라디언트 면적 채우기, ③ 각 지점에 작은 점(마지막 지점은 더 크고 밝은 색으로 강조).

**구현 중 자체 발견 — 연출 종료 타이밍 카운터 누락.** 이미 통과한 폰에서 연출이 끝나는 시점은 "애니메이션이 걸린 요소 개수(`animated.length`)만큼 `animationend`를 받으면 종료"하는 방식인데, `wrap`에 붙은 `animationend` 리스너는 선택자로 걸러 듣지 않고 **그 아래에서 발생하는 모든 `animationend`를 다 센다.** 새 요소(주 눈금 격자, 면적 채우기, 지점 점들)에 애니메이션을 추가하면서 이 카운트 목록(`animated`)도 같이 늘리지 않으면, 실제보다 훨씬 적은 이벤트만으로 `pending`이 0이 되어 **새로 추가한 요소들의 애니메이션이 채 끝나기도 전에 화면이 넘어가 버리는** 문제가 생길 뻔했다. 구현 직후 코드를 다시 읽다가 발견해 `animated`의 선택자 목록에 새 요소들을 모두 추가했다.

**추가로 자체 발견 — 동작 줄이기에서 꺾은선이 아예 안 보이는 기존 결함.** 이번 작업 중 재확인하다가, 꺾은선(`path`)은 `opacity`가 아니라 `stroke-dashoffset`(1→0)으로 그려지는데, `prefers-reduced-motion` 규칙은 `animation:none`만 걸고 `stroke-dashoffset`은 그대로 뒀다는 것을 발견했다 — 애니메이션을 꺼도 인라인으로 박아 둔 `stroke-dashoffset:1`(선이 하나도 안 그려진 상태)이 그대로 남아, **동작 줄이기 기기에서는 꺾은선 자체가 안 보였을 가능성이 있다.** 이번에 `#dnk-gate .bg-line path{stroke-dashoffset:0!important;}`를 같은 미디어쿼리에 추가해 막았다. (5차 이전부터 있던 결함으로 보이며, 이번 회차와 직접 관련은 없지만 같은 파일을 손보다가 발견해 바로 고쳤다.)

**검증.** 새 탭에서 게이트를 띄워 격자·주 눈금·면적 채우기·지점 점이 모두 그려진 최종 상태를 스크린샷으로 확인.

**재발방지.** `animationend` 개수를 세어 연출 종료를 판단하는 코드에 새 애니메이션 요소를 추가할 때는, **그 요소를 카운트 목록에도 반드시 같이 넣을 것** — 리스너가 `wrap` 전체에서 이벤트를 받고 있어 목록과 실제 이벤트 발생 수가 어긋나도 에러 없이 조용히 타이밍만 틀어지기 때문에 알아채기 어렵다.

### [4] 챗봇 검색 — 엔터를 쳐도 반응이 없어 보임 — 위험도: 낮음 (5차가 만든 회귀로 추정)

**문제.** 검색어를 입력하고 엔터를 쳐도 아무 반응이 없다는 보고. 재현을 시도한 결과, 매칭 로직 자체(`matchLiveAnalysis`)는 정상 동작했다 — "ok 마스터 샘플 트렌드가 어때?" 문장을 넣으면 실시간 분석 카드가 정상적으로 만들어진다.

**원인분석(유력).** 5차에서 폰 키보드가 챗봇 창을 가리는 문제([4-6]항목 참고)를 고치면서, 검색칸(`#helpSearch`)에 **포커스**가 가면 패널을 위로 옮기고(`kb-avoid` 추가) **포커스가 빠지면**(`blur`) 원래 위치(화면 아래)로 되돌리게 만들었다. 그런데 폰에서 검색어를 입력한 뒤 키보드의 "검색/완료" 버튼을 누르면, 그 자체가 입력칸의 **blur를 유발**하는 경우가 많다. 그러면 결과가 막 만들어진 바로 그 순간 패널이 사용자가 보고 있던 화면 위쪽에서 아래로 튕겨 내려가 버려, **결과는 실제로 나왔는데 사용자 눈에는 "반응이 없다"처럼 보였을 가능성이 크다.** 즉 5차의 키보드 회피 기능 자체가 이번 "먹통" 증상의 원인일 개연성이 높다.

**조치.** `blur`에서 패널을 원래 위치로 되돌리는 로직을 없애고, **패널을 닫을 때(`closeHelp`)** 한 번만 위치를 초기화하도록 옮겼다. 패널이 열려 있는 동안에는 검색칸에서 포커스가 빠져도(엔터를 치거나 다른 곳을 눌러도) 이미 위로 옮겨진 위치를 그대로 유지한다.

**검증.** 이 자동화 환경에서는 가상 키보드의 "검색" 버튼을 실제로 누르는 상황을 완전히 재현할 수는 없어(엔터 키 입력만으로는 blur가 발생하지 않았다), 코드 경로상의 원인 제거를 위주로 검증했다 — `input` 이벤트만으로 실시간 분석 카드가 뜨는 것은 재확인했고, `blur` 리스너 자체를 없앴으므로 이제 어떤 이유로 blur가 발생하든 패널 위치는 패널을 닫기 전까지 바뀌지 않는다.

**재발방지.** 포커스/블러로 UI 위치·상태를 토글하는 기능을 폰에서 만들 때는, **"블러가 왜 발생하는가"를 하나로 단정하지 말 것** — 사용자가 다른 곳을 눌러서만이 아니라, 가상 키보드의 확인/검색 버튼을 누르는 것 자체도 blur를 유발할 수 있다. 이 경우 "포커스가 빠지자마자 되돌리는" 로직이 "방금 만들어진 결과를 사용자 눈앞에서 치워버리는" 부작용으로 이어질 수 있다.

### [5] 주간/월간 보기 버튼 — 여전히 "안 보인다"는 재보고 — 위험도: 낮음 (배치 개선)

**문제.** 5차에서 실배포본에 코드가 정상 존재함을 `curl`로 확인하고 캐시 문제로 결론 냈는데, 같은 문제가 다시 보고됨(사진 첨부).

**재확인.** 로컬 시험에서 실제 등록된 공정(`eq=120`, "조립전 유로계") 화면으로 재현해 보니, 토글 버튼(`#periodSeg`) 자체는 정상 렌더링되고 있었다 — 다만 위치가 **페이지 맨 위(설비 정보 카드 바로 아래)** 뿐이라, 사용자가 보낸 사진처럼 ②그래프 카드까지 스크롤해서 내려간 상태에서는 화면 밖으로 벗어나 눈에 안 들어온다. 코드 결함이라기보다 **배치가 안 좋아 "안 보인다"는 인상을 준** 경우로 판단.

**조치.** 페이지 맨 위 토글은 그대로 두고, **②그래프 카드 안(제목 바로 아래)에도 같은 토글을 하나 더** 추가했다(`#periodSeg2`). 두 토글은 `setPeriod()`에서 함께 갱신되고, 클릭 리스너도 둘 다에 걸려 있어 어느 쪽을 눌러도 동일하게 동작한다.

**검증.** `#periodSeg2`가 그래프 카드 안에 정상 렌더링됨을 확인. `#periodSeg2`의 "주간" 버튼을 클릭하면 `#periodSeg`(페이지 맨 위)의 "on" 상태도 함께 "주간"으로 바뀌고, 기록표 제목도 "주별 기록"으로 바뀌는 것을 확인 — 두 토글이 하나의 상태를 공유함을 검증.

**재발방지.** 페이지 상단에만 있는 전역 컨트롤이 특정 섹션의 표시를 바꾸는 구조에서는, 그 섹션까지 스크롤한 사용자 입장에서 **그 컨트롤이 화면 밖에 있지 않은지**를 항상 점검할 것. "버튼이 안 보인다"는 보고를 캐시 문제로 단정하기 전에, 실제 그 사용자가 보고 있었을 스크롤 위치를 먼저 재현해 볼 것 — 5차에서는 이 단계를 건너뛰고 코드 존재 여부만 `curl`로 확인해 캐시로 결론 낸 것이 성급했다.

### [6] 「기타사항」 잠금 해제가 안 된다는 보고 — 위험도: 중간 (재현 실패, 방어적 조치)

**문제.** 「기타사항」으로 잠긴 상태에서 접속 암호를 입력해도 잠금이 풀리지 않는다는 보고. 모바일·PC 모두에서 재현된다고 하며 "심각함"으로 표시됨.

**재현 시도.** 로컬 시험 서버가 `leak-sync.js`에 박힌 **실제 운영 Apps Script 주소**를 그대로 바라보고 있어(로컬 시험이 실서비스와 통신하는 것 자체는 [9]에서 이미 알려진 특성), 시험 중 실제로 운영 데이터를 한 번 읽어 왔다 — 그 결과 `calibHidden:"Y"`(실제로 잠긴 상태)가 로컬 화면에 그대로 재현됐다. **쓰기(재잠금 해제 결과 반영)는 `window.dnkSync.enabled = ()=>false`로 강제로 막아 둔 채**, 이 실제 잠금 상태에 대해 실제 접속 암호(`dnkm8721`)로 `tryCalibReveal()`을 그대로 호출해 봤는데, **정상적으로 풀렸다**(`calibTitle`이 "디텍터 보정 이력"으로, `calibHiddenView.hidden`이 `true`로 바뀜). 해시 비교 로직(`window.dnkGate.passHash`/`sha256Hex` 참조), 게이트 쪽 값 자체(`PASS_HASH` = `sha256("dnkm8721")`) 모두 코드상 문제를 찾지 못했다.

**조치(방어적).** 근본 원인을 특정하지 못한 채로 재현에 실패했지만, 비밀번호 입력값에 `.trim()`을 적용하지 않고 있던 것은 사실이라 **모바일 자동완성·자동교정이 앞뒤 공백을 붙여넣는 경우를 막기 위해** 이 시스템의 비밀번호 관련 입력 3곳(게이트 1차 암호, 「기타사항」 해제 암호, 관리자 암호) 모두에 `.trim()`을 추가했다. 정확한 원인은 아닐 수 있으나, 실패할 이유가 없는데 그냥 밑져야 본전인 안전한 개선이라 함께 반영했다.

**재발방지 및 남은 과제.** 이 항목은 **재현에 실패한 채로 방어적 조치만 반영한 상태**다. 재배포 후에도 다시 안 풀리면, 정확히 어떤 메시지가 뜨는지("암호가 올바르지 않습니다" vs "게이트 정보를 읽지 못했습니다" vs 아무 반응 없음)와 그 순간 사용한 정확한 문자열(복사/자동완성 여부 포함)을 알려달라고 요청할 것 — 지금은 이 정보가 없어 추가로 좁힐 수 없었다.

---

## 2026-09-19 (5차) PC 그래프 좌측 쏠림 · NG 점 호버 · 챗봇 매칭 · 게이트 세션 전환

사용자가 한 번에 9개 항목을 보고함. 그중 코드 조치가 필요했던 것만 이 문서에 남기고, 캐시 문제로 판단한 나머지는 재발방지 항목에 기록.

### [1] PC(넓은 화면)에서 게이트 배경 꺾은선이 왼쪽에 쏠림 — 위험도: 낮음 (사용성)

**문제.** 게이트 애니메이션의 배경 꺾은선(`.bg-line`, `<svg>`)이 `position:absolute;left:8%;right:8%`로만 폭을 지정하고 `width`는 없었다. 폭이 좁은 폰 화면에서는 눈에 안 띄었지만, PC처럼 넓은 화면에서는 그림이 화면 왼쪽 절반 정도에만 그려지고 오른쪽은 빈 채로 남았다.

**원인분석.** `<svg>`는 고유 비율(`viewBox="0 0 200 60"`)을 가진 "교체 요소(replaced element)"라, CSS가 `width`를 명시적으로 주지 않으면 `left`/`right`로 정한 폭을 따르지 않고 **`viewBox` 비율과 이미 정해진 `height`만으로 폭을 역산**해 버린다. 실제로 1280px 너비 화면에서 이 SVG가 533px 지점에서 잘려 있는 것을 `getBoundingClientRect()`로 확인했다.

**조치.** `.bg-line` 규칙에 `width:84%`를 명시적으로 추가.

**검증.** 480px(폰)·1280px(PC) 두 뷰포트에서 `getBoundingClientRect()`로 실측 — 1280px에서 SVG가 left=101.19~right=1163.78(폭 1062.59px)로 의도한 `left:8%~right:92%` 범위를 거의 그대로 채우고, 화면 중심에서 좌우 오차 7.5px 이내로 정렬됨을 확인.

**재발방지.** `position:absolute`/`fixed`인 `<svg>`·`<img>`·`<video>` 같은 "교체 요소"에 `left`/`right`(또는 `top`/`bottom`)만으로 폭·높이를 정하려 할 때는, **반드시 `width`(또는 `height`)도 같이 명시할 것** — 고유 비율이 있는 요소는 누락된 축을 자기 비율로 역산해 버려 레이아웃이 화면 크기에 따라 다르게 깨질 수 있다.

### [2] 시업 전 점검 — 덮어쓰기 안내 문구가 눈에 안 띔 — 위험도: 낮음 (사용성)

**문제.** "이미 기록이 있습니다. 저장하면 덮어씁니다"라는 경고성 문구가 다른 안내 문구와 같은 회색 톤이라 주의를 끌지 못한다는 지적.

**조치.** 덮어쓰는 상황(기존 기록이 있는 일자·교대를 고른 경우)에서만 `.hint-warn` 클래스를 붙여 적색 굵은 글씨 + 옅은 적색 배경 박스로 강조. 새 일자·교대로 바꾸면 클래스가 빠지고 원래 문구·스타일로 돌아간다.

**검증.** 브라우저에서 기존 기록이 있는 일자·교대를 고르면 `hasWarnClass:true`, `color:"rgb(184, 69, 58)"`, `fontWeight:"800"`으로 렌더링됨을 확인. 새 일자·교대로는 `hasWarnClass:false`, 기본 문구로 돌아감을 확인.

### [3] 트렌드 그래프 NG 점에 호버 강조 효과 추가 — 위험도: 낮음 (기능 추가)

**요청.** NG로 판정된 점은 마우스를 올리면 적색으로 흐려졌다 진해지는 효과를 보여 달라는 요청. 이어서 "폰에서도 호버 효과를 나타낼 부분이 있는가"라는 질문도 함께 받음.

**조치.** 그래프 렌더링 시 NG 점에 `class="pt-ng"`를 붙이고, `@media (hover:hover) and (pointer:fine)` 안에서만 `:hover`에 펄스 애니메이션(`ngPulse`, 0.9초 반복)을 걸었다. 이 미디어쿼리 없이 걸면 터치 기기에서 점을 한 번 탭한 뒤에도 hover 상태가 남아(sticky hover) 계속 깜빡이는 것처럼 보일 위험이 있어, QR 정보관리 시스템이 쓰는 것과 같은 가드를 그대로 썼다. `prefers-reduced-motion`에서는 애니메이션을 끔.

**폰(터치 기기)에는 이 효과를 안 만든 이유.** `:hover`는 마우스처럼 "가리키기만 하고 누르지 않는" 상태가 있는 입력 장치에서만 자연스럽게 동작한다. 터치 기기는 그런 중간 상태가 없어 :hover를 억지로 흉내 내면 탭 이후에도 상태가 남는 부작용이 생긴다. 대신 이 그래프에는 이미 "점을 탭하면 값·판정이 말풍선으로 뜨는" 기능(`attachChartTouch`)이 있어, **그게 이미 폰에서의 사실상 동일한 역할**을 하고 있다고 판단해 별도로 만들지 않았다.

**검증 중 시행착오 — CSS 안에 JS 스타일 주석(`//`) 사용.** 설명 주석을 달다가 실수로 `<style>` 문자열 안에 `//`로 시작하는 줄 주석을 썼다 — CSS는 `/* */`만 유효하다. 편집 직후 재확인 과정에서 스스로 발견해 `/* */` 블록 주석으로 고쳤고, `<style>` 블록 전체의 중괄호 짝(136 대 136)과 `//` 잔존 여부를 스크립트로 재검사해 0건임을 확인.

**검증.** 스펙(상한 4.0)을 설정한 뒤 초과값(5.5)으로 기록을 저장 → 그래프에 `.pt-ng` 점이 정확히 1개 렌더링되고 저장 토스트도 "NG 1건"으로 뜸을 확인. `window.matchMedia('(hover:hover) and (pointer:fine)')`가 데스크톱 환경에서 `true`, `(pointer:coarse)`가 `false`로 나와 호버 규칙이 걸리는 환경임을 확인. CSS 규칙 트리를 재귀로 순회해 `.pt-ng:hover{animation:...}`가 실제로 `@media (hover:hover) and (pointer:fine)` **안에** 있는 것도 소스로 직접 재확인.

**재발방지.** 호버 효과의 첫 검증 스크립트가 `document.styleSheets`를 얕게(1단계만) 순회해 `@media`로 감싸진 규칙의 media 조건을 잘못된 위치(안쪽 `CSSStyleRule`)에서 찾는 바람에 "미디어쿼리로 안 감싸져 있다"는 오탐이 한 번 났다. **중첩된 `@media` 안의 규칙을 검증할 때는 `CSSMediaRule.cssRules`까지 재귀로 내려가서 확인할 것** — cssText 문자열 매칭만으로는 어느 중첩 단계에 있는지 알 수 없다.

### [4] 챗봇 "OK마스터샘플 트렌드 분석" 매칭 재실패 — 위험도: 낮음 (회귀)

**문제.** 직전 회차(4차)에서 이미 한 번 고쳤다고 안내한 실시간 분석 매칭이 "여전히 안 된다"는 재보고를 받음.

**원인분석.** 직전 수정은 검색어를 **단어 단위로 점수화**해 FAQ 매칭 자체는 개선했지만, 실시간 분석의 **샘플명 대조**(`matchLiveAnalysis()`)는 여전히 원문 그대로 부분 일치를 보고 있었다. 사용자의 실제 입력 "OK마스터샘플 트렌드 분석을 해줘"(붙여씀)는 샘플명 "OK 마스터"(띄어씀)를 문자열 그대로는 포함하지 않아 매칭에 실패했다 — 지난 회차의 조치 범위가 문제의 다른 절반(샘플명 대조)을 놓친 것.

**조치.** 검색어·샘플명 양쪽에 공백 제거 + 소문자 변환을 적용하는 `normForMatch()`를 추가해 그 값으로 대조하도록 변경.

**검증.** Node로 격리한 단위시험에서 "OK마스터샘플 트렌드 분석을 해줘" → "OK 마스터" 매칭 성공을 확인. 브라우저에서도 같은 문장을 실제로 입력해 `📈 "OK 마스터" 실시간 분석 결과` 카드가 뜨는 것을 확인.

**재발방지.** 사용자 입력을 여러 단계(문장 매칭 → 개체명 대조 등)에 걸쳐 처리하는 기능을 고칠 때는, **보고된 실패 사례가 어느 단계에서 걸리는지 끝까지 추적할 것** — 앞 단계만 고치고 "고쳤다"고 안내했다가 뒷단계의 같은 종류 문제로 재보고를 받은 사례.

### [5] 폰에서 챗봇 검색칸 포커스 시 키보드가 위젯을 가림 — 위험도: 낮음 (사용성)

**문제.** 도움말 위젯(`position:fixed`, 화면 하단 고정)을 폰에서 열고 검색칸을 누르면, 화면 키보드가 올라오면서 위젯 자체가 가려져 안 보이는 문제.

**원인분석.** `position:fixed`/`absolute`로 고정된 요소는 화면 키보드가 줄여 놓은 "실제 보이는 영역"이 아니라, **키보드와 무관한 원래 레이아웃 뷰포트 기준**으로 위치가 잡히는 경우가 많다(기기·브라우저에 따라 다름). 그래서 하단에 고정해 둔 위젯이 키보드 뒤로 밀려 들어간 것처럼 가려진다.

**조치.** 터치 포인터 기기(`matchMedia("(pointer:coarse)")`)에서만, 검색칸(`#helpSearch`)에 포커스가 가면 위젯에 `.kb-avoid` 클래스를 붙여 화면 **위쪽**으로 옮기고, 포커스가 빠지면 원래 위치로 되돌린다. PC(마우스)에서는 이 분기를 타지 않는다 — 키보드가 화면을 가리는 문제 자체가 없기 때문.

**검증.** 375×812(모바일) 뷰포트로 에뮬레이션한 뒤, 실제 마우스 클릭으로 검색칸을 포커스해 `helpPanel.className`이 `"kb-avoid"`로 바뀌는 것과, 위젯이 화면 상단으로 이동해 검색칸이 가려지지 않는 것을 스크린샷으로 확인.

**검증 중 시행착오 — 자동화 탭이 백그라운드일 때 focus 이벤트가 안 옴.** 처음에는 JS의 `.focus()` 호출만으로 검증하려 했는데, 자동화 브라우저 탭이 화면 앞으로 와 있지 않으면(`document.hasFocus()===false`) `focus`/`blur` 이벤트 자체가 발생하지 않아 "기능이 고장 난 것"처럼 보였다. 탭을 앞으로 가져온 뒤(`tabs_select`) 실제 마우스 클릭으로 다시 시도하니 정상 동작 확인.

**재발방지.** 포커스/블러 기반 기능을 자동화로 검증할 때는 **탭이 실제로 전경(포그라운드)에 있는지(`document.hasFocus()`) 먼저 확인할 것** — 배경 탭에서의 `.focus()` 호출은 브라우저에 따라 포커스 이벤트를 발생시키지 않을 수 있어, 실패 원인을 코드 쪽으로 오인할 수 있다.

### [6] 게이트 인증 유지 방식을 QR 정보관리 시스템과 동일하게 전환 — 위험도: 낮음 (요건 변경, 트레이드오프 있음)

**경위.** 원래(2026-09-16 신설 시)는 "QR 코드 목적 자체가 찍을 때마다 즉시 입력 화면이므로, 매번(새 탭마다) 다시 묻는 QR 시스템 방식은 이 프로젝트에 안 맞는다"는 판단으로 `localStorage`(기기당 평생 1회)를 썼다. 이번에 "QR 코드 찍고 게이트 애니메이션 뒤 비밀번호창이 안 나온다, QR 정보관리 시스템과 동일하게 해 달라"는 명시적 요청을 받아 그 판단을 뒤집었다.

**조치.** `gate.js`의 `isUnlocked()`/`markUnlocked()`를 `localStorage` → `sessionStorage`로 전환. 암호 카드 안내 문구도 "이 기기에서 처음 한 번만 확인합니다"에서 "이 화면을 닫지 않는 동안에는 다시 묻지 않습니다 / QR을 다시 찍거나 새로 열면 한 번 더 확인합니다"로 갱신.

**트레이드오프(기록해 둘 것).** 같은 탭 안에서 새로고침하는 정도는 다시 안 물어보지만, **QR을 다시 찍어 새 탭이 열리거나 앱을 완전히 종료했다 재실행하면 매번 다시 암호를 입력해야 한다** — 하루에 QR을 여러 번 찍는 운영이라면 이전보다 손이 더 간다. 이 트레이드오프를 감수하고서라도 QR 시스템과의 일관성을 택한 것이 이번 결정이다.

**검증.** 새 탭(`tabs_create`)으로 열면 암호 입력칸이 뜨고(`sessionKey:null`) 통과 전임을 확인. 같은 탭에서 새로고침만 하면 게이트가 다시 뜨지 않고(`gateExists:false`) `sessionKey:"1"`이 유지됨을 확인 — `iframe`으로는 `sessionStorage`가 부모 탭과 공유되어 "새 탭" 상황을 제대로 흉내 낼 수 없어, 진짜 새 탭을 띄워 검증했다.

**재발방지.** 이후 다시 "한 번만 물어보게" 요청이 오면 `localStorage`로 되돌리면 된다 — 이번 전환의 근거와 코드는 이 항목과 `gate.js` 상단 주석에 남아 있다. 같은 결정을 두 번 뒤집을 때는 **이전 판단의 근거를 지우지 말고 "그 판단을 왜 뒤집었는지"를 덧붙여 남길 것** — 다음에 또 뒤집을 상황이 오면 무엇을 잃고 무엇을 얻는지 바로 알 수 있다.

### [7] 주간/월간 보기 버튼이 안 보인다는 보고 · 보정 이력 암호가 안 풀린다는 보고 — 위험도: 낮음 (원인: 배포 캐시로 추정)

**문제.** 두 기능 모두 직전 회차(4차, 커밋 `4e675a9`)에서 이미 구현·배포되었는데도 "안 보인다"/"안 풀린다"는 보고를 받음.

**원인분석.** `curl`로 실서비스(`https://dnk-mobility.github.io/eawd-leak-management/`)의 배포된 `index.html`을 직접 받아 확인 — 주간/월간 전환 코드(`periodSeg`)와 최신 관리자 암호 해시(`CALIB_HASH`, `dnkm8721*`에 대응)가 **이미 정상적으로 배포되어 있음**을 확인했고, Node의 `vm.Script`로 문법 오류도 없음을 확인했다. 즉 서버에 올라간 코드 자체는 문제가 없다.

**결론(추정).** 코드 결함이 아니라, GitHub Pages의 캐시(`Cache-Control: max-age=600`, 배포 후 10분) 또는 사용하는 폰 브라우저 자체의 캐시가 원인일 가능성이 가장 높다 — 이 프로젝트에서 여러 차례 반복된 패턴([9]·이전 회차 기록 참고).

**조치.** 별도 코드 수정 없음. 사용자에게 폰에서 **새로고침(길게 눌러 "캐시 무시하고 새로고침" 또는 브라우저 설정에서 사이트 데이터 삭제)** 후 재확인을 안내.

**재발방지.** "방금 배포했는데 안 보인다"는 보고를 받으면, 코드를 다시 고치기 전에 **먼저 `curl`로 실배포본에 해당 코드가 실제로 들어있는지부터 확인할 것** — 코드가 이미 정상이라면 캐시 문제일 가능성이 높고, 이 경우 손댈 코드가 없으므로 사용자에게 캐시 삭제를 안내하는 것이 맞는 조치다.

---

## 2026-09-19 (4차) 게이트 가독성 · 주간/월간 보기 · 보정 이력 암호 안내

### [1] 게이트 애니메이션에서 글자가 배경 그림에 가려 안 보임 — 위험도: 낮음 (사용성)

**문제.** 꺾은선 SVG(`.bg-line`)가 화면 정중앙(`top:38%~62%`)을 지나가도록 되어 있어, 그 자리에 뜨는 카드 글자와 정확히 겹쳤다. 격자·스캔선까지 겹치면 글자가 잘 안 보인다는 피드백.

**조치.** 두 가지를 같이 했다.

1. 꺾은선을 카드 아래쪽(`top:68%~86%`)으로 옮겨 애초에 겹치지 않게 함.
2. 그래도 격자·스캔선은 카드 자리를 지나가므로, 카드 뒤에 **타원형 그라디언트 음영**(`.bg-scrim`)을 깔았다 — 중심부(카드 자리)는 배경색에 가깝게 진하고, 가장자리로 갈수록 투명해져 격자 무늬와 자연스럽게 섞인다.

**구현 중 자체 발견.** 음영 요소의 평소(애니메이션 전) 상태가 `opacity:0`인데, `prefers-reduced-motion:reduce` 규칙에서 "애니메이션을 끄고 최종 상태로 고정"하는 목록에 이 요소를 빠뜨리면 **음영이 영영 opacity:0인 채로 남는다** — 동작 줄이기 기기에서만 이 개선이 무효화되는 상황이 될 뻔했다. reduced-motion 규칙에 `.bg-scrim`도 `opacity:1!important`로 포함시켜 막았다.

**재발방지.** 새 시각 요소를 추가할 때, 애니메이션의 "시작 상태"(대개 `opacity:0`)가 있다면 **`prefers-reduced-motion` 규칙에도 그 요소를 같이 넣었는지** 항상 확인할 것 — 안 그러면 모션을 줄인 사용자에게만 보이는 회귀가 조용히 생긴다.

### [2] 누설값 트렌드 그래프 — 주간/월간 보기 추가 (기능 추가)

②그래프·③기록표를 월 단위 외에 **주 단위**(월요일~일요일)로도 볼 수 있게 했다. 상세 설계는 `PROJECT_OVERVIEW.md` §3-3.

**설계에서 짚어 둘 점 — 월 경계를 걸치는 주.** 예를 들어 9/28~10/4 같은 주는 9월·10월 두 달에 걸쳐 있다. 처음 짠 동기화 코드는 "지금 보고 있는 달(`ym`) 하나만" 서버에서 받아오는 구조였는데, 이 전제를 그대로 두면 **주가 걸친 두 번째 달의 서버 데이터가 영영 안 내려와** 그 폰에 이미 있던(어쩌면 오래된) 로컬 데이터만 보이는 문제가 생긴다. `periodMonths()`로 "지금 화면에 필요한 달 목록"(1개 또는 2개)을 구하고, `syncNow()`가 그 목록 전부를 병렬로 pull 하도록 고쳤다. `applyPulled()`의 "이 응답이 아직 유효한가" 판정도 `ymAt === ym`(정확히 같은 달인가)에서 `periodMonths()`에 포함되는가로 바꿔야 했다 — 안 그러면 두 번째 달의 pull 응답이 도착해도 "필요 없는 달"로 오인해 버려진다.

**검증.** 날짜 계산 함수(월요일 계산·주 끝·월/연도 경계)를 실제 데이터 없이 단위시험으로 먼저 검증한 뒤, 브라우저에서 월 경계를 걸치는 주(8/31~9/6, 8월+9월 데이터 혼재)로 이동해 기록표·그래프에 양쪽 달 데이터가 함께 나오는 것, 그래프 제목·CSV·PNG 파일명에도 기간이 반영되는 것, 월간으로 되돌아왔을 때 그 주가 속한 달로 정확히 매핑되는 것까지 확인.

**테스트 중 자체 발견(재확인).** 페이지가 이미 로드된 상태에서 `localStorage`를 직접 덮어써도 화면에 반영되지 않아 한동안 기능이 깨진 줄 알았다 — 이미 여러 번 겪은 "인메모리 `db`는 새로고침해야 localStorage 변경을 다시 읽는다"는 함정에 이번에도 걸렸다. 데이터를 바꿔서 시험할 때는 **반드시 새로고침 후, 게이트 연출이 끝나기를 기다렸다가** 조작할 것.

### [3] 보정 이력 암호 입력칸 두 개가 헷갈림 — 위험도: 낮음 (사용성)

**문제.** ⑤ 섹션에 암호 입력칸이 두 개 있다 — 「기타사항」 잠금 해제용(접속 암호, `dnkm8721`)과 관리자 모드 전환용(별도 암호, `dnkm8721*`). 둘 다 그냥 "암호 입력"이라고만 표시되어 있어 어느 걸 넣어야 하는지 헷갈린다는 문의를 받음.

**조치.** 각 입력칸 라벨을 구체화("접속 암호"/"관리자 암호")하고, 밑에 "QR 찍고 들어올 때 쓰는 접속 암호와 같은 암호입니다" / "접속 암호와는 다른 암호입니다" 라는 한 줄 안내를 추가했다.

**재발방지.** 같은 화면에 성격이 다른 암호 입력칸이 2개 이상 있으면, **라벨만으로는 부족하고 반드시 "어느 암호인지" 한 줄을 덧붙일 것.** 만든 사람에게는 당연해 보여도(관리자 암호와 접속 암호를 각각 언제 만들었는지 기억하고 있으므로), 처음 보는 사용자에게는 구분이 안 된다.

---

## 2026-09-19 (3차) 보정 이력 접근 권한 재설계 + 「기타사항」 잠금

### [1] 조회/수정 권한 분리 — 위험도: 낮음 (요건 변경)

**경위.** 직전 구현은 "관리자만 입력·열람"이라는 첫 요청에 따라 **섹션 전체**를 2차 암호로 잠갔다. 이후 "평소에는 누구나 확인 가능하게 하고 수정만 관리자 한 사람"으로 요건이 구체화됨.

**조치.** 읽기/쓰기를 분리 — 이력 표와 CSV 내보내기는 잠금과 무관하게 항상 열려 있고, **입력 폼과 삭제 버튼만** 2차 암호로 잠근다. 잠긴 상태에서는 표의 "삭제" 열 자체가 렌더링되지 않는다(버튼만 숨기는 게 아니라 열을 통째로 뺌).

**기록해 둘 한계.** 계정 체계가 없는 정적 사이트라 "특정 한 사람만"은 결국 **그 사람만 아는 암호**로 구현할 수밖에 없다. 암호를 누군가에게 알려주는 순간 그 사람도 수정 권한을 갖게 된다 — 시스템이 사람을 식별하는 것이 아니라 암호를 아는지만 확인하기 때문. 진짜 개인 단위 권한·감사 기록이 필요해지면 계정 체계(백엔드)가 필요하다.

### [2] 「기타사항」 잠금 신설 — 위험도: 낮음 (기능 추가)

**요청.** 버튼 하나로 "디텍터 보정 이력" 제목을 "기타사항"으로 바꾸고 내용을 감추는 기능. 해제 암호는 게이트(접속) 암호와 동일.

**설계 판단 2가지.**

1. **적용 범위** — "그 폰에서만"과 "모든 사람에게" 중 확인 결과 **모든 사람에게**. 그래서 상태를 로컬이 아니라 구글시트("설정" 시트)에 두고 `pull()` 응답에 항상 실어 보낸다. 설비와 무관한 앱 전체 설정이라 기존 per-eq 구조와 별개로 `db.settings`를 새로 뒀다.
2. **해시 중복 회피** — 해제 암호가 게이트 암호와 같아야 하는데, 해시 값을 `index.html`에 복사해 두면 나중에 게이트 암호를 바꿀 때 한쪽만 바뀌어 **조용히 어긋나는** 사고가 난다. `gate.js`가 `window.dnkGate.passHash`/`sha256Hex`로 넘겨주고 `index.html`은 그걸 받아 쓰도록 했다.

**구현 중 자체 발견 — 감췄는데 DOM에는 남아 있던 문제.** 처음에는 감춤 처리를 컨테이너 `hidden`으로만 했는데, 그러면 **이전에 그려 둔 표 내용이 DOM에 그대로 남아** 개발자도구로 바로 보였다. 감추는 것이 목적인 기능에서 내용이 남아 있으면 의미가 옅어지므로, 감출 때 `calibList`·`calibEditor`의 내용을 비우도록 수정.

**성격에 대한 기록.** 해제 암호가 작업자 모두가 아는 게이트 암호이므로, 이건 **"외부인 눈에 안 띄게" 하는 수준**이지 내부 권한 구분이 아니다. 내부 권한 구분은 [1]의 관리자 암호가 담당한다. 두 장치의 목적이 다르다는 점을 혼동하지 말 것.

**검증.** 잠금 → 제목 전환·내용 비워짐·설정 저장, 오답 거부, 게이트 암호로 해제 → 원상 복구, 새로고침 후에도 잠금 유지까지 실제로 재현해 확인.

**재발방지(시험 절차).** 이번 시험 중 **브라우저가 캐시된 `gate.js`를 쓰는 바람에** 새로 추가한 `passHash`가 안 보여 한동안 코드 버그로 오인했다. 정적 파일을 고친 뒤 브라우저로 시험할 때는 `fetch(파일, {cache:'reload'})` 등으로 **캐시를 먼저 갱신**하고 시작할 것 — 이 세션에서 같은 원인으로 두 번 헤맸다.

**서버 반영:** `Code.gs`에 "설정" 시트와 `setting` 액션 추가 — **재배포 필요**.

### [3] 관리자 암호를 `calib-4720` → `dnkm8721*` 로 변경 — 위험도: 낮음 (의도된 트레이드오프)

**경위.** 처음 구현 때는 "1차 암호를 유추할 수 있는 형태면 2차 암호의 의미가 없다"는 판단으로 무관한 값(`calib-4720`)을 발급했다. 이후 요청으로 `dnkm8721*`(1차 접속 암호 + `*`)로 변경.

**판단.** 이 값은 **QR 정보관리 시스템의 2차 암호(`HASH_2`)와 정확히 같은 값**이다 — 즉 임의로 고른 것이 아니라, 자매 시스템에 이미 있는 규칙에 맞춘 것이다. 두 시스템에서 "한 단계 더 민감한 자료"를 여는 암호가 하나로 통일되면 담당자가 외울 것이 줄어든다.

**알고 쓸 것(기록).** 1차 암호에 문자 하나를 붙인 형태라 **1차 암호를 아는 사람이 유추하기 어렵지 않다.** 즉 "관리자만 수정"의 실효성은 암호의 강도가 아니라 *굳이 시도하지 않는다*는 신뢰에 기대게 된다. 이 시스템 전체가 "실제 보안 장치가 아니라 우연한 열람을 막는 수준"이라는 전제(§한계) 위에서 **외우기 쉬움을 택한 결정**이며, 실제로 수정 권한을 엄격히 통제해야 할 상황이 오면 유추 불가능한 값으로 바꿔야 한다.

---

## 2026-09-19 (2차) 기능 추가 4건 — QR번호·통계 용어 명시·챗봇 검색 개선·보정 이력 이관

배경: 실사용 중 나온 요청 4건을 한 번에 처리. ①~③은 작고 독립적이라 바로 반영, ④(보정 이력 이관)는 QR 정보관리 시스템이 남긴 `인계_마스터샘플_보정이력.md`을 참고해 설계.

### [1] 챗봇 검색이 자연스러운 문장에 반응하지 않음 — 위험도: 낮음 (기능 결함)

**문제.** 도움말 위젯에 "OK 마스터 트렌드 분석해줘"라고 입력해도 아무 결과가 안 나옴.

**원인.** `searchFaq()`가 "입력한 문장 전체가 FAQ 텍스트 안에 그대로 포함되는가"만 검사했다. 그 정확한 문장을 담은 FAQ가 없는 한, 자연스러운 문장형 질문은 구조적으로 항상 0건이었다 — 이건 특정 버그가 아니라 검색 방식 자체의 한계였다.

**조치.**

1. `searchFaq()`를 단어 단위 토큰화 + 겹침 점수 방식으로 교체 — 짧은 키워드 검색과 자연스러운 문장 검색 둘 다 어느 정도 대응 가능해짐.
2. 한 걸음 더 나아가, "트렌드"·"분석"·"이상"·"신호" 같은 분석 의도 단어와 **현재 채널의 실제 샘플명**이 함께 검색되면, 미리 쓴 답을 찾는 대신 §추이 이상 탐지 엔진(`detectTrend`)을 그 샘플의 실제 데이터로 그 자리에서 실행해 진짜 분석 결과를 보여주도록 함(`matchLiveAnalysis`/`renderLiveAnalysis`). 이미 있는 통계 계산을 재사용하는 것이라 추가 비용 없음.

**검증.** 원래 실패했던 문장("OK 마스터 트렌드 분석해줘")으로 실제 데이터를 세팅해 재현 — 실시간 분석 카드가 뜨고, [전체 이력 보기로 이동] 버튼까지 정상 동작함을 확인. 존재하지 않는 샘플명·무관한 검색어에서는 실시간 카드가 안 뜨는 것도 함께 확인.

**재발방지.** 키워드 검색 UI를 만들 때는 **"사용자가 실제로 어떻게 타이핑할지"를 먼저 가정하고 시험할 것** — 개발자는 짧은 키워드로 시험하기 쉽지만, 실사용자는 완전한 문장으로 입력하는 경우가 많다.

### [2] 추이 이상 신호의 통계 수치에 라벨이 없어 이해하기 어려움 — 위험도: 낮음 (사용성)

**문제.** "표준편차 1.757" 같은 숫자가 무엇을 뜻하는지 라벨이 없어 헷갈린다는 피드백.

**조치.** "경고 구간 근접"·"변동성 급증" 두 규칙의 근거(`detail`) 문장에서 **"표준편차"라는 용어를 명시하고 괄호로 짧은 정의**를 붙임("값이 평소 얼마나 흩어져 있는지 나타내는 통계 수치"). "경고 구간 근접"은 추상적인 "2배" 대신 **실제 경계값**(예: "위쪽 경계는 3.296 mL/min")도 계산해서 보여주도록 개선.

**재발방지.** [3](이전 항목, 같은 날 앞쪽)에서 이미 "쉬운 말이 먼저, 숫자는 보조"로 구조를 나눴는데도 이 문제가 남아 있었던 이유는 **보조(근거) 줄 안에서도 라벨 없는 숫자는 여전히 불친절하기 때문**이다. "기술적인 내용이니 숫자만 보여주면 된다"고 안일하게 판단하지 말 것 — 근거 줄도 처음 보는 사람이 이해할 수 있어야 진짜 근거로 기능한다.

### [3] QR 코드 번호 필드 추가 (기능 추가, 위험도 없음)

④마스터 샘플 · 스펙 관리의 샘플명 옆에 "QR 코드 번호" 입력칸 추가(`s.qrNo`). 그 마스터 샘플(물리 시편)에 붙여 둔 QR 라벨을 기록해 두는 용도. `Code.gs`의 "마스터샘플" 시트에 열이 추가되어 **재배포가 필요**(구글시트_연동_설정방법.md 참고).

### [4] 디텍터 보정 이력을 QR 정보관리 시스템에서 이관 — 위험도: 중간 (접근 제어 설계)

**배경.** QR 정보관리 시스템은 정적 페이지라 값을 계속 누적 입력할 수 없어, 그 시스템에 있던 "마스터 샘플·보정 이력" 섹션(디텍터 보정 전/후 값 기록)을 삭제하고 이 시스템으로 이관하기로 함(그 저장소의 `인계_마스터샘플_보정이력.md`).

**설계 결정 — 접근 제어.** 인계 문서는 "일반 조회보다 한 단계 높은 접근 제한"만 권고하고 구체적 방법은 위임했다. 이번 요청은 더 구체적으로 "관리자 한 사람만 수정 가능하게" 였음 — 이 시스템에는 계정·권한 체계가 없어(공용 암호 방식), **그 사람만 아는 비밀번호**가 "특정 한 사람만"을 구현하는 유일한 현실적 방법이라고 판단. QR 시스템의 2차 암호 패턴(별도 해시, `sessionStorage`)을 그대로 가져오되, 새 비밀번호(`calib-4720`)를 새로 발급했다 — 1차 암호를 재사용하면 2차 암호를 두는 의미가 없어지기 때문(가능성으로만 검토하고 채택하지 않음). **→ 이 비밀번호는 이후 `dnkm8721*`로 변경됨(아래 3차 [3] 참고).**

**설계 결정 — 채널·판정.** 원본은 "OK 마스터(하우징)"처럼 샘플명에 채널을 접미사로 붙였는데(그때는 채널이 안 나뉘어 있었음), 이 시스템은 이미 130-h/130-i로 채널이 나뉘어 있어 샘플을 드롭다운으로 선택하는 것으로 충분했다. 판정(OK/NG)은 원본이 완전 수동이었던 것을, 선택한 샘플의 스펙과 리크값-후를 비교해 자동 제안(수동 확인 가능)하도록 개선.

**미반영 사항 — 실데이터 2건.** 인계 문서에 남아 있던 실제 이력 2건(2026-09-02, 130-h·130-i)은 **자동으로 옮기지 않았다.** 확인자 칸이 원래 공란이었고, 인계 문서 자체가 "담당자명은 실제 작업자에게 확인 후 채워 넣어야 한다"고 명시했기 때문 — 확인 없이 그대로 복사하면 잘못된 확인자 정보(공란)를 그대로 이관하는 셈이 된다. `PROJECT_OVERVIEW.md` §4-7에 원본 값을 표로 남겨 두었으니, 관리자가 직접 한 번 입력하면서 확인자를 채워 넣는 것을 권장.

**검증.** 오답/정답 암호 처리, 판정 자동 제안(스펙 이탈 시 NG로 전환), 저장 후 폼 초기화(샘플·확인자는 유지), 이력 표 렌더링, CSV 내보내기(수식 삽입 방지 적용 확인), 채널 전환 시 2차 잠금 유지·이력 데이터는 채널별로 분리, 삭제 확인창까지 전부 실제 데이터로 재현해 확인.

**서버 반영:** `Code.gs`에 새 시트 "보정이력"과 `calibPush` 액션 추가 — **재배포 필요** (구글시트_연동_설정방법.md 참고, 위 QR 코드 번호 변경사항과 함께 한 번에 반영됨).

---

## 2026-09-19 추이 이상 신호 자동 탐지 기능 신설

배경: "AI가 데이터를 학습해서 최적화하는 기능을 무료로 넣을 수 있나"는 질문에 실현 가능성을 검토한 결과, 무료 조건과 이 시스템의 데이터 규모(설비당 월 수십 건, 원인 기록 없음) 양쪽 다 실제 머신러닝을 지지하지 않는다고 판단. 대신 제조업 통계적 공정관리(SPC)에서 쓰는 규칙 기반 탐지로 "추이 이상 조짐 포착 + 흔한 원인 안내"라는 실질적 요청을 충족하기로 하고 구현. 상세 설계는 `PROJECT_OVERVIEW.md` §4-5.

### [0] 구현 중 자체 발견 — 부동소수점 오차로 안정적인 값이 "쏠림"으로 오탐될 뻔함 — 위험도: 중간 (배포 전 발견)

**문제.** "평균 한쪽 쏠림" 규칙이 `vals[i] > 평균` 비교로 판정하는데, 완전히 동일한 값(예: 3.2가 8번 반복)만 있어도 신호가 뜨는 것을 자체 단위시험에서 발견함.

**원인.** `mean([3.2,3.2,...,3.2])`를 IEEE 754 부동소수점으로 계산하면 `3.1999999999999997`처럼 **정확히 3.2가 아닌 값**이 나올 수 있다. 그러면 원래 값 `3.2`가 계산된 평균보다 "크다"고 잘못 비교된다 — 가장 안정적인(변화가 전혀 없는) 데이터가 역설적으로 "쏠림 신호"를 내는 최악의 오탐이었다.

**발견 경위.** 기능을 배포하기 전, "완전히 동일한 값만 반복" 케이스를 포함한 자체 단위시험을 짜서 실행하다 발견. 사용자에게 보고되기 전에 잡음.

**조치.** 계산된 통계량(평균·표준편차)과 원본 값을 비교하는 모든 자리에, 실제 측정 정밀도(소수 3자리)보다 훨씬 작은 여유(`TREND_EPS = 1e-9`)를 두어 부동소수점 노이즈를 흡수하도록 수정.

**검증.** 동일 값 반복·거의 동일하지만 실제로 다른 값(0.001~0.002 차이)·정상 30일치 무작위 데이터 세 가지 모두 의도대로(오탐 없음, 진짜 차이는 그대로 감지) 동작하는 것을 재확인.

**재발방지.** **계산으로 얻은 부동소수점 값(평균·표준편차·합계 등)을 원본 입력값과 `>`/`<`/`===`로 직접 비교하는 코드를 새로 짤 때는, 반드시 이 여유값 패턴을 먼저 떠올릴 것.** 특히 "완전히 안정적인/동일한 입력"이 실제로 자주 나오는 도메인(이 시스템처럼 같은 기준 시편을 반복 측정하는 경우)에서는 이런 입력이 그냥 예외 케이스가 아니라 **오히려 가장 흔한 정상 케이스**이므로 우선적으로 시험해야 한다.

### [1] 규칙 설계 — 판정 대상을 기준값 계산에서 제외해야 하는 이유

**문제(초기 설계 결함, 구현 중 발견).** "경고 구간 근접"(최근 3회 중 2회가 평균±2σ 바깥) 규칙을 처음에는 **전체 값(판정 대상 3개 포함)**으로 평균·표준편차를 계산해 판정했다.

**원인분석.** 극단값 2개가 포함된 상태로 평균·표준편차를 구하면, 그 극단값 자체가 표준편차를 크게 넓혀 버린다. 예: 평소 3.2 근처에서 안정적이던 값이 갑자기 5.5~5.6으로 튀어도, 그 5.5~5.6이 계산에 포함되면 "평소 변동폭"이 실제보다 훨씬 넓게 계산되어 **정작 그 튄 값 자체가 "정상 범위 안"으로 판정되는 역설**이 생긴다. 실제로 이렇게 짠 첫 버전에서 이 시나리오를 단위시험으로 넣어 봤더니 신호가 전혀 뜨지 않아 발견했다.

**조치.** 평균·표준편차를 **판정 대상 3개를 뺀 나머지("기준값")로만** 계산하도록 수정. 방금 든 예시로 재시험하니 정상적으로 신호가 뜨는 것을 확인.

**재발방지.** 이상 탐지 규칙을 설계할 때는 **"판정 기준"과 "판정 대상"이 같은 데이터를 공유하면 안 된다**는 원칙을 항상 먼저 확인할 것 — 기준 자체가 이상치에 오염되면 그 이상치를 못 잡는다.

### [2] 구현·시험까지 마친 기능을 커밋하지 않아, 배포가 안 된 채로 사용자가 "왜 안 되냐"고 문의 — 위험도: 낮음 (작업 프로세스)

**문제.** 이 기능을 로컬에서 구현하고 단위시험·브라우저 통합시험까지 전부 통과시킨 직후, 커밋·푸시를 하지 못한 상태로 작업이 끊겼다. 사용자가 실제 현장 데이터(9/1~9/17)를 입력하고 라이브 사이트에서 기능을 찾았으나 당연히 보이지 않았고, "기능이 작동 안 하냐"는 문의를 받았다.

**발견 경위.** 문의를 받고 `git status`로 로컬 변경사항이 커밋되지 않은 채 남아 있는 것과, 라이브 사이트에 `detectTrend` 코드 자체가 없는 것을 확인해 원인을 바로 특정함.

**조치.** 즉시 커밋·푸시하고 라이브 반영을 확인.

**재발방지.** **"구현 + 로컬 검증 완료"와 "커밋·배포 완료"는 별개다 — 작업 세션이 어떤 이유로든 끊길 수 있다는 전제 하에, 기능 단위로 작업이 일단락되면 바로 커밋·푸시까지 마치는 것을 원칙으로 한다.** 특히 "검증까지 끝났으니 이제 커밋만 하면 된다"고 생각하는 시점이 오히려 위험하다 — 실제로 아직 안 끝난 상태이기 때문이다.

### [3] 통계 용어 노출로 현장 작업자가 이해하기 어려움 — 위험도: 낮음 (사용성)

**문제.** 신호 설명 문구에 "표준편차 1.757" 같은 통계 용어와 원값을 그대로 노출해, "작업자가 힘들어할 수 있다"는 피드백을 받음.

**조치.** 각 신호를 `plain`(통계 용어 없이 무엇이·왜 문제인지 풀어 쓴 주 문장)과 `detail`(날짜·수치 등 근거, "근거: " 접두어로 작게 보조 표시) 두 단으로 나눔. 변동성 급증 규칙은 배율 표현("이전보다 약 X배")으로 바꾸되, 기준 구간이 우연히 거의 평평하면 배율이 비현실적으로 커지는 문제(예: 70배)가 있어 5배 이상은 "5배 이상으로"라고 뭉뚱그리도록 추가 보정.

**검증.** 극단적 케이스(기준 구간 표준편차가 0에 가까움)와 현실적 케이스(2~3배 수준) 둘 다 문구가 자연스럽게 나오는 것을 재확인.

**재발방지.** 현장 작업자를 대상으로 하는 화면 문구에 통계·수학 용어(표준편차, σ, 신뢰구간 등)를 쓸 때는 **"숫자를 몰라도 무엇을 해야 하는지 알 수 있는 한 문장"을 먼저 쓰고, 검증용 숫자는 보조 정보로 분리**할 것.

---

## 2026-09-19 시스템 전면 재점검 — 보안·기능

배경: 퇴사자 발생 시 재차 문의하기로 하고, "지속적으로 관리·유지해야 하니 보안·기능 전반을 꼼꼼히 점검하고, 이력·원인분석·재발방지가 기록되게 해 달라"는 요청에 따라 실시. `index.html`(1,788줄) · `gate.js` · `leak-sync.js` · `Code.gs` · `styles.css` 전체를 다시 읽고, 사용자 입력이 거쳐 가는 모든 경로(화면 렌더링 · CSV 내보내기 · 구글시트 기록)를 추적해 점검함.

### 이상 없음으로 확인된 항목

| 항목 | 결과 |
|---|---|
| 화면 렌더링(XSS) | 샘플명·확인자·비고 등 사용자 입력이 화면에 나오는 모든 자리에서 `esc()`로 이스케이프됨. 예외 없음 |
| 되돌릴 수 없는 작업 | 기록 삭제·샘플 삭제·전체 삭제(2단계 확인)·백업 덮어쓰기 전부 확인창 있음 |
| 외부 의존성 | CDN·외부 폰트·분석 스크립트 등 제3자 리소스 전혀 없음 (전부 자체 호스팅) |
| 크롤러 차단 | `robots.txt`(전면 차단) + 전 페이지 `noindex` 메타 이중 적용 — QR 시스템의 과거 실측 교훈이 처음부터 반영되어 있었음 |
| 모바일 기본기 | 뷰포트 확대 허용, 입력창 16px(iOS 자동 확대 방지) 이미 적용 |
| 동시 저장 충돌 | `Code.gs`가 `LockService.getScriptLock()`으로 저장·삭제를 직렬화함 |
| 인증 토큰 일관성 | `leak-sync.js`·`Code.gs` 양쪽 TOKEN이 일치, 모든 액션이 토큰 검사를 거침 |

---

### [1] 구글시트 수식 삽입(Formula Injection) — 위험도: 높음

**문제.** `Code.gs`의 `push()`/`saveSamples()`가 샘플명·확인자·비고 같은 자유 텍스트를 검증 없이 `Range.setValues()`로 그대로 시트에 썼다. 구글시트는 API로 넣은 문자열도 사람이 직접 타이핑한 것과 동일하게 취급해서, 값이 `=`·`+`·`-`·`@`로 시작하면 **수식으로 해석**한다.

**발견 경위.** 사용자 입력이 최종적으로 도달하는 곳(구글시트 셀)까지 데이터 흐름을 추적하다가 확인. `leak-sync.js`의 TOKEN이 GitHub Pages로 공개 서빙되는 파일 안에 있다는 점(문서화된 기존 한계)까지 함께 고려하면, 앱 화면을 거치지 않고 토큰만 알아내 직접 요청을 보내는 경로도 이론적으로 가능함을 확인.

**위험.**

- **실수로도 발생한다.** 작업자가 "비고"란에 "-5도 낮음"이나 "=완료" 같은 평범한 문장을 그대로 입력해도 그 칸이 깨지고(수식 오류 또는 의도와 다른 계산 결과), 원인을 모른 채 "왜 이 칸만 이상하지"로 남는다.
- **악의적으로는 더 심각하다.** `=IMPORTXML(...)`·`=IMPORTDATA(...)` 같은 함수는 시트가 열릴 때 **외부 서버로 실제 네트워크 요청을 보낼 수 있어**, 관리자가 시트를 여는 순간 다른 셀 값이 외부로 유출되는 경로가 될 수 있다.

**조치.** `Code.gs`에 `safeText(v)` 함수를 추가 — 값이 `=`/`+`/`-`/`@`/탭으로 시작하면 앞에 어퍼스트로피(`'`)를 붙여 시트가 **항상 텍스트로만** 읽게 만든다(어퍼스트로피 자체는 셀에 표시되지 않음). `push()`·`saveSamples()`가 시트에 쓰는 모든 문자열 필드(공정No., 일자, 교대, 각종 ID, 샘플명, 구분, 판정, 확인자, 비고, 폐기일자)에 적용했고, 숫자 필드(누설값·지정값·상하한·설비주변온도)는 `asNum()`으로 한 번 더 강제해 문자열이 숫자 칸에 섞여 들어갈 수 없게 했다.

**검증.** `safeText("=IMPORTXML(...)")`, `safeText("-5도 낮음")`, `safeText("+82")`, `safeText("@메모")` 가 모두 앞에 `'`가 붙는 것을, 정상 값("홍길동", "정상 비고")은 그대로인 것을 별도 스크립트로 확인.

**재발방지.** 사용자 입력이 **수식 해석 규칙을 가진 곳**(구글시트, 엑셀로 열리는 CSV 등)에 도달하는 자리를 새로 만들 때는 항상 이 패턴을 먼저 떠올릴 것 — "이 값이 `=`로 시작하면 어떻게 되는가?"

---

### [2] CSV 내보내기 수식 삽입 — 위험도: 중간

**문제.** [1]과 같은 근본 원인의 다른 경로. `exportCsv()`의 `csvCell()`이 따옴표·쉼표·줄바꿈만 이스케이프하고, `=`/`+`/`-`/`@`로 시작하는 값은 그대로 내보냈다. 이 CSV는 "엑셀이 한글을 안 깨뜨리도록 UTF-8 BOM을 붙인다"는 주석대로 **애초에 엑셀에서 열 것을 전제**로 만들어져 있어, 노출 조건이 [1]보다도 쉽게 충족된다(관리자가 월말 백업 삼아 CSV를 열기만 해도 됨).

**위험도가 [1]보다 낮은 이유.** 이 값이 실행되려면 관리자가 **내려받은 CSV 파일을 직접 열어야** 한다(구글시트처럼 서버에 저장되는 즉시 살아있는 문서가 되는 게 아님). 그래도 실제 운영 절차(`구글시트_연동_설정방법.md`의 "평소 사용 방법") 자체가 CSV를 정기적으로 열어 보게 되어 있어 노출 가능성은 낮지 않다.

**조치.** `csvCell()`에 [1]과 동일한 검사·어퍼스트로피 처리를 추가.

**검증.** 실제로 확인자란에 `=1+1`, 비고란에 `-공격시도`를 저장한 뒤 CSV를 내보내, 두 값 모두 `'=1+1` · `'-공격시도`로 나가는 것을 확인(엑셀에서 수식이 아닌 문자 그대로 보임).

---

## 2026-09-19 게이트(암호 화면) 정밀 점검 — QR 정보관리 시스템 벤치마킹

배경: "QR 정보관리 시스템처럼 보안·비밀번호 관련 시스템을 벤치마킹해 달라"는 요청으로, 그 시스템이 남긴 [`인트로_암호게이트_구현상세.md`](../후공정%20설비%20QR%20코드%20스마트폰%20관리%20시스템/후공정%20설비%20정보%20시스템%20프로젝트/인트로_암호게이트_구현상세.md)를 기준으로 이 시스템의 `gate.js`를 대조 점검함. 관련 커밋: `200974b`, `9add70e`.

### [3] gate.js에 평문 비밀번호가 주석으로 노출 — 위험도: 높음

**문제.** `PASS_HASH` 상수 바로 옆에 `// dnkm8721 (기존 QR 시스템과 동일 암호)`라는 주석이 있었다. `gate.js`는 인증 없이 누구나 받아볼 수 있는 공개 파일이라, **소스 보기 한 번**으로 두 시스템의 비밀번호가 그대로 드러났다. SHA-256 해시를 쓰는 것 자체가 무의미해지는 상태였다.

**발견 경위.** QR 시스템의 `gate.js`는 해시만 있고 평문 주석이 없다는 점을 대조하다가 발견.

**원인.** 처음 암호를 정할 때 "QR 시스템과 같은 암호"라는 사실을 남겨 두려고 습관적으로 적어 둔 주석이, 실제로는 인증 파일 자체를 공개 배포한다는 사실과 충돌한다는 걸 놓침.

**조치.** 주석 제거. `PASS_HASH` 값(해시)만 남김.

**검증.** 배포 후 실제 서비스 URL에서 `gate.js`를 직접 받아 `dnkm8721` 문자열이 0건인 것을 확인.

**재발방지.** **정적 사이트에서 서빙되는 파일은 전부 "누구나 읽는 파일"**이라고 전제할 것. 코드 옆 주석에 실제 값(비밀번호·키·개인정보)을 남기지 말고, "왜 이 값인지"만 적을 것.

---

### [4] 모바일 "동작 줄이기" 기기에서 게이트 화면이 통째로 안 뜨는 것처럼 보임 — 위험도: 중간 (기능 결함)

**문제.** "QR을 찍을 때마다 게이트 애니메이션이 항상 나왔으면 좋겠다"는 요청을 반영하면서, "이미 통과한 기기 + `prefers-reduced-motion`(동작 줄이기) 켜짐"이면 연출을 건너뛰고 즉시 앱으로 넘어가는 예외를 **자체 판단으로 추가**했다. 실제 현장 폰 중 이 설정이 켜진 기기에서 애니메이션도 카드도 전혀 안 뜨고 앱 내용이 바로 보이는 증상으로 나타나, 사용자가 "게이트가 고장났다"고 보고함.

**발견 경위.** 사용자 실사용 보고. 화면 상태(앱 내용이 바로 보임)와 기기 이력(이전에 통과한 적 있음)을 확인해 원인을 좁힘.

**원인분석.** 요청은 "항상"이었는데, 접근성을 배려한다는 이유로 요청받지 않은 예외 조건을 임의로 추가한 것이 근본 원인. "동작 줄이기"는 배터리 절약 모드에서 자동으로 켜지는 경우가 있어 **본인도 모르게 켜져 있는 경우가 흔하다** — 이 설정 하나로 화면 전체를 건너뛰는 분기는 실사용 환경에서 예측 불가능한 결과를 낳기 쉽다.

**조치.** 해당 예외 분기를 완전히 제거. 이제 `prefers-reduced-motion`이 켜져 있어도 게이트 화면 자체는 항상 뜨고, CSS의 `@media (prefers-reduced-motion:reduce)`가 격자·스캔·선·카드의 **전환 애니메이션만** 정적으로 바꾼다(그림 자체는 그대로 나옴).

**검증.** 코드에서 해당 분기 자체를 삭제했으므로 JS 레벨에서 구조적으로 재발 불가능. 동일 시나리오(이미 통과한 기기)를 iframe으로 재현해 화면이 항상 표시되는 것을 확인.

**재발방지.** **사용자가 명시적으로 "항상"이라고 말한 요구사항에, 요청받지 않은 예외를 임의로 추가하지 않는다.** 접근성 등 다른 가치와 상충할 소지가 있다고 판단되면 예외를 조용히 넣지 말고 먼저 확인을 구할 것.

---

### [5] 고정 타이머로 종료 시점을 잡아 "너무 빠르다" 피드백 — 위험도: 낮음 (사용성)

**문제.** 게이트 연출이 끝나는 시점을 `setTimeout(fn, 1600)`처럼 **고정 시간**으로 재고 있었다. 첫 조치 후에도 "여전히 빠르다"는 피드백을 받음.

**원인분석.** QR 시스템 문서 §3.4가 지적한 것과 같은 함정 — 요소가 "또렷해지는 시점"만 신경 쓰고 **또렷한 상태로 멈춰 있는 시간**(읽을 시간)을 별도로 확보하지 않았음. 등장 시간(duration)을 아무리 늘려도 멈춰 있는 시간이 그대로면 체감 속도는 안 바뀐다. 추가로, 고정 타이머는 로드가 느릴 때 마지막 동작이 잘리거나, 나중에 delay·duration을 조정하면 "무엇이 가장 늦게 끝나는지"가 바뀌어 또 깨지는 구조적 약점이 있었다(§3.5).

**조치.**

1. `TAIL_MS`(연출이 다 끝난 뒤 정지 시간, 900ms) · `FADE_MS`(페이드아웃, 700ms) · `TIMEOUT_MS`(안전 종료, 6000ms) 3개 상수로 분리 — 속도 조정은 `TAIL_MS`부터.
2. 고정 타이머 대신 `animationend` 이벤트가 몇 번 왔는지 **개수를 세어** 전부 끝난 뒤 `TAIL_MS`만큼 쉬는 방식으로 교체.
3. 백그라운드 탭·동작 줄이기 기기는 `animationend`가 아예 안 올 수 있어(브라우저가 애니메이션을 정지시키거나 CSS로 꺼버림), 이 경우에만 시간 기반 폴백을 쓰도록 분리.

**효과.** 전체 길이 2.0초 → **3.4초**, 글자가 또렷하게 머무는 구간 0.5초 → **1.6초**. 로드 지연·기기 성능과 무관하게 마지막 동작이 잘리지 않음이 구조적으로 보장됨.

**검증.** `animationend`를 인위적으로 발생시켜 카운팅 로직이 정확히 `TAIL_MS` 후 페이드를 시작하고 `FADE_MS+60ms` 후 화면을 넘기는 것을 실측 확인.

---

### [6] `<noscript>` 방어 누락 — 위험도: 중간

**문제.** 게이트가 JS로 화면을 가리는 방식이라, **브라우저에서 자바스크립트를 끄면 게이트 자체가 존재하지 않게 되어** 본문이 그대로 노출됐다. QR 시스템은 이 상황에 대비해 `<noscript>`로 본문을 숨기고 안내문을 띄우는 처리가 있었는데, 이 시스템에는 없었다.

**조치.** `index.html`의 `<body>` 시작 부분에 동일한 방식의 `<noscript>` 블록 추가 — JS가 꺼져 있으면 본문을 `visibility:hidden`으로 숨기고 "자바스크립트가 필요합니다" 안내를 띄운다.

**한계(기록, QR 시스템 문서 §11과 동일).** 완전한 차단은 아니다 — 개발자 도구로 `gate.js`를 직접 열면 로직과 해시가 그대로 보이고, 8자리 영숫자 암호는 오프라인 대입으로 복원 가능하다. 이 시스템은 "우연히/장난으로 들여다보는 것"을 막는 수준이며, 실제로 지켜야 할 것(측정 원본 데이터)의 방어선은 구글시트 자체의 공유 권한이라는 점은 QR 시스템 문서와 같은 결론이다.

---

## 2026-09-16~18 채널 확장 · 샘플 이력 관리 · 구글시트 연동

배경: 초기 구축 이후 실사용 과정에서 나온 요청·문제들을 반영. 관련 커밋: `23b7f0c`, `8921976`, `b5dbc25`, `c3fd0ec`, `acf136b`, `88b8822`, `be40806`, `61ea2d4`.

### [7] 마스터 샘플 폭 넓힘 입력칸이 실제 폰에서 "mL/min" 단위를 밀어냄 — 위험도: 낮음 (사용성)

**문제.** 첫 조치(`flex:0 1 50%`만 지정)가 로컬 브라우저 시험에서는 정상이었지만, 실제 사용자 폰에서는 여전히 단위 표시가 오른쪽 경계를 벗어난다는 보고를 받음.

**원인분석.** 두 가지 가능성을 함께 고려함 — (a) GitHub Pages의 캐시 헤더(`max-age=600`)로 배포 직후 10분간 이전 파일이 보일 수 있음, (b) 일부 모바일 브라우저에서 숫자 입력창의 **네이티브 스피너(▲▼) 버튼**이 `flex-basis` 계산과 별개로 시각적 폭을 잠식할 수 있음. 원인을 하나로 단정하지 않고 둘 다 대응함.

**조치.** `max-width`/`width`/`min-width:0`을 중복 지정해 폭 계산을 이중으로 고정하고, `-webkit-appearance:textfield` + `::-webkit-inner/outer-spin-button{-webkit-appearance:none}`으로 네이티브 스피너 자체를 제거.

**검증.** 실제 픽셀 좌표로 측정 — 입력칸 146.5px(정확히 카드 폭의 50%), 단위 라벨이 카드 우측 경계보다 110px 안쪽에 위치함을 확인.

**재발방지.** 모바일 렌더링 문제는 **로컬 브라우저 시험만으로 "해결됨"이라 단정하지 말 것** — 사용자의 실제 기기 재확인을 요청하고, 캐시·기기별 렌더링 차이 양쪽 가능성을 열어 둘 것.

---

### [8] 색상 테마 전환 시 하드코딩 hex 1건 누락 — 위험도: 낮음

**문제.** 시스템 전체 색상을 네이비+시안 계열에서 크림+틸 계열로 바꾸는 작업에서, `styles.css`의 `.doc-type` 규칙에 있던 `#8fb3d6`(옛 팔레트의 하늘색) 하나가 새 팔레트 값으로 바뀌지 않고 남아 있었다.

**발견 경위.** 색상 교체 완료 후 검증 단계에서, 옛 팔레트에서 쓰던 hex 값 14개를 전부 나열해 `grep`으로 재검색 — 1건 남아 있음을 발견.

**조치.** `#9fc4c0`(새 팔레트의 대응 색)로 교체 후 재검색해 0건 확인.

**재발방지.** CSS 변수(`:root`)로 관리되지 않는 하드코딩 색상(JS로 그리는 SVG, 인라인 스타일)이 있는 프로젝트에서 팔레트를 바꿀 때는, **옛 값 목록으로 최종 재검색**하는 단계를 항상 거칠 것 — "다 바꿨다"는 느낌만으로 끝내지 않기.

---

### [9] 로컬 시험 트래픽이 실제 운영 구글시트에 반영됨 — 위험도: 중간 (반복 발생)

**문제.** `leak-sync.js`의 연동 주소가 상대 경로가 아닌 **절대 URL**이라, `http://127.0.0.1`로 로컬 시험을 해도 실제 운영 Apps Script/시트로 요청이 그대로 나간다. 동기화 기능 검증 중 1회(의도된 검증), 이후 폐기/재개 기능의 UI 시험 중 1회(의도치 않은 부작용) — 총 2회 발생.

**발견 경위.** 매번 `action=pull` 직접 호출로 사후 점검하는 습관 덕분에 그때그때 발견.

**조치.** 발견 즉시 해당 설비(`eq`)에 대해 빈 배열로 덮어써 정리하고, 재조회로 깨끗해진 것을 확인.

**재발방지.** 로컬 서버로 시험할 때는 **매 시험 세션 시작 전에 `window.dnkSync.enabled = function(){return false;}`로 동기화를 강제로 꺼 두는 것을 기본 절차로 삼는다** (이번 회차 점검부터 실제로 이 방식을 적용함). 시험이 끝나면 실제 운영 주소에 `action=pull`을 날려 오염 여부를 재확인하는 습관도 계속 유지.

---

### [10] Apps Script 재배포 직후 일시적 "파일을 열 수 없습니다" — 위험도: 낮음 (원인 오인 위험)

**문제.** 사용자가 배포 관리 화면에서 "새 버전"으로 재배포를 완료한 직후, 실제 서비스 URL(`/exec`)이 구글 드라이브의 "현재 파일을 열 수 없습니다" 오류를 반환함. 계정·배포 ID·파일 존재 여부를 모두 재확인했으나 문제가 없어 원인을 좁히지 못할 뻔함.

**원인분석.** 배포 관리 화면의 스크린샷으로 배포 ID를 대조하는 과정에서, **대문자 I와 소문자 l이 화면 폰트에서 거의 구분되지 않아** 다른 ID로 착각할 뻔했다(실제로는 완전히 같은 ID). 이후 시간을 두고 재확인하니 정상 응답 — 재배포 직후 반영까지의 **일시적 전파 지연**이 원인이었음.

**조치.** 별도 조치 없음(대기 후 재확인으로 해결). 사용자에게 "잘못하신 게 아니다"라고 명확히 안내.

**재발방지.** 스크린샷으로 ID·해시 등 영숫자 문자열을 대조할 때는 **대문자 I / 소문자 l / 숫자 1**, **대문자 O / 숫자 0** 같은 혼동 쌍을 염두에 두고, 가능하면 실제 동작(API 호출 등)으로 재검증할 것 — 육안 비교만으로 "다르다"고 단정하지 않기.

---

## 2026-09-15~16 시스템 신설 · 계정 구조 설계

배경: QR 정보관리 시스템과 별개로 신설. 관련 커밋: `ad772f7`, `2557631`.

### [11] 개인 계정에 시스템이 묶이는 구조적 위험 — 위험도: 높음 (선제 조치)

**문제.** GitHub 저장소·구글 계정 모두 특정 개인 명의로 만들어지면, 그 사람이 퇴사할 때 인수인계가 어렵거나 시스템이 통째로 접근 불가능해질 위험이 있음.

**조치.**

1. GitHub: 개인 계정 저장소를 `dnk-mobility` 조직으로 이전(`gh api -X POST repos/{owner}/{repo}/transfer`), 저장소명을 `<org>.github.io`로 변경해 루트 URL 자동 배포 유지. QR 정보관리 시스템도 같은 이유로 함께 이전.
2. 구글 계정: 개인 계정 대신 **부서 명의 신규 계정**(`dnkm959@gmail.com`)을 만들어 구글시트·Apps Script를 여기 귀속시킴. 복구 이메일만 현재 담당자 개인 메일로 두고, **담당자 교체 시 계정을 새로 만들지 않고 복구 이메일만 교체**하는 방식으로 설계.

**효과.** 담당자 개인의 재직 여부와 무관하게 두 시스템 모두 조직·부서 명의로 유지됨.

**재발방지 관점.** 사내 시스템을 새로 만들 때는 **"이 계정 소유자가 내일 그만두면 어떻게 되는가"**를 설계 초기에 항상 자문할 것.

---

## 문서 작성 원칙 (참고)

- 위험도는 "실제 보안 장치가 아니다"라는 이 시스템의 근본 한계(`PROJECT_OVERVIEW.md` §4·`구글시트_연동_설정방법.md` "알아 둘 한계") 안에서의 상대적 우선순위이며, 절대적인 침해 가능성 등급이 아니다.
- "재발방지"는 다음에 비슷한 코드를 작성하거나 비슷한 상황을 마주쳤을 때 **먼저 떠올려야 할 질문·습관**을 적는 것을 목표로 한다 — 이미 끝난 조치의 재서술이 아니다.
- **오류·문제가 실제로 생기면(사용자 보고든 구현 중 자체 발견이든) 예외 없이 이 문서에 원인분석·재발방지까지 남기고 나서 조치를 마무리한다** — "간단한 수정이니 굳이 안 남겨도 된다"는 판단은 하지 않는다(2026-09-19(7차), 명시적 요청으로 재확인).
