# e-AWD 모터 하우징 누설값 관리 시스템 — 프로젝트 개요

> 후공정 생산기술팀 · e-AWD 70kW MOTOR HOUSING ASS'Y 라인
> 2026-09-16: 기존 "공정 설비 정보관리 시스템"(dongnam959.github.io, QR코드 설비 정보시스템)에서 완전히 분리해 신설. 두 시스템은 코드·저장소·작업 폴더가 전부 별개다.
> 2026-09-18: 전체계(130·190) 채널을 하우징/인버터로 분리해 4채널 → 6채널로 확장. 하단 §3-1 참고.
> **문제 발견·원인분석·조치 이력은 [`개선이력.md`](개선이력.md)에 누적 기록한다.** 이 문서는 "지금 시스템이 어떻게 되어 있는가"를, `개선이력.md`는 "왜 지금 이렇게 되었는가"를 담당한다.

---

## 1. 목적

리크테스터 4개소(공정No.120·130·180·190, 그 중 130·190은 하우징·인버터 2채널씩이라 화면상 6개 채널)가 매일 시업 전 OK/NG 마스터 샘플로 검사기 정상 여부를 확인하고 누설값을 기록하는 작업을, 종이 기록지 대신 QR 스캔 → 스마트폰 입력으로 대체한다. 저장과 동시에 합격/불합격 판정과 추이(꺾은선) 그래프가 자동으로 나온다.

기존 QR 정보관리 시스템과는 **완전히 독립된 별도 저장소**다. 분리 이유: QR 정보관리 시스템은 "찍으면 문서를 읽는" 용도라 인트로(4.5초)·2단계 암호 등 진입 절차가 있고, 이 프로젝트는 "찍으면 바로 입력"이 핵심이라 그 절차들이 방해가 된다.

---

## 2. 배포 구조

| 항목 | 내용 |
|---|---|
| 로컬 개발 환경 | `C:\Users\user\Desktop\후공정 리크 설비 마스터 샘플 기록관리 프로젝트` |
| 저장소 | [`dnk-mobility/eawd-leak-management`](https://github.com/dnk-mobility/eawd-leak-management) |
| 브랜치 | `main` (단일 브랜치) |
| 배포 방식 | GitHub Pages (project site — QR 정보관리 시스템과 달리 `<조직명>.github.io` 루트가 아니라 `<조직명>.github.io/<저장소명>/` 하위 경로) |
| 실서비스 주소 | https://dnk-mobility.github.io/eawd-leak-management/ |
| 빌드 도구 | 없음 — 순수 정적 HTML/CSS/JS |
| 저장소 생성 | 2026-09-16, GitHub CLI(`gh repo create`)로 `dongnam959` 개인 계정에 생성 |
| 조직 이전 | 2026-09-16, `dnk-mobility` 조직(Organization)으로 이전. 개인 계정(`dongnam959`) 하나에 계정이 묶이면 담당자 퇴사 시 인수인계가 어려워지는 문제를 없애기 위함 — 상세 사유는 §9 참고 |

---

## 3. 파일 구성

| 파일 | 역할 |
|---|---|
| `tools/make_qr.py` | QR 재생성 스크립트 (조직 이전 등으로 주소가 바뀔 때 `BASE` 값만 고치고 재실행) |
| `index.html` | 전체 화면 1장. 채널 전환 탭 6개(`?eq=120\|130-h\|130-i\|180\|190-h\|190-i`) + 입력·그래프·월별기록·스펙관리·백업 |
| `gate.js` | 접속 암호 — **탭을 닫을 때까지만** 확인 유지(sessionStorage). 2026-09-19(5차)부터 QR 정보관리 시스템과 동일한 방식(§4) |
| `leak-sync.js` | 구글 스프레드시트 연동 — 폰 저장 우선, 그 다음 전송(§5) |
| `styles.css` | QR 정보관리 시스템에서 그대로 가져온 공용 스타일 (id-bar·section·표 등) |
| `favicon.svg` | 탭 아이콘 (QR 정보관리 시스템과 동일 디자인) |
| `robots.txt` | 검색엔진 수집 차단 |
| `tools/apps-script/Code.gs` | 구글 시트 쪽 서버 코드 원본 (Apps Script 편집기에 배포된 것과 동일본 — 백업/참고용). **코드를 고치면 Apps Script 편집기에도 재배포해야 실제로 반영된다** (`구글시트_연동_설정방법.md` 참고) |
| `구글시트_연동_설정방법.md` | 최초 1회 연동 설정 절차 (계정 소유자가 직접 수행) |
| `qr-codes/eq-120.png` 등 6개 | 채널별 QR (§6) |

---

## 3-1. 채널 구성 (2026-09-18, 4채널 → 6채널)

리크테스터는 물리적으로 4개소(공정No.120·130·180·190)지만, **130·190(전체계)은 하우징·인버터 두 부위를 한 설비에서 순서대로 검사**한다. 처음에는 이 둘을 한 화면(한 채널)에서 같이 관리했는데, 마스터 샘플이 4개(OK/NG × 하우징/인버터)가 한 탭에 섞여 있어 그래프가 겹쳐 보기 불편했다. 그래서 **하우징과 인버터를 서로 다른 채널로 완전히 분리**했다 — 이제 130·190은 각각 2개 채널(`-h`/`-i`)을 갖는다.

| 채널 키(`?eq=`) | 화면 탭 이름 | 비고 |
|---|---|---|
| `120` | 조립전 유로계 | 그대로 |
| `130-h` | 조립전 전체계(하우징) | **신규 분리** — 이전엔 `130` 하나에 하우징·인버터 샘플 4개가 같이 있었음 |
| `130-i` | 조립전 전체계(인버터) | **신규 분리** |
| `180` | 조립후 유로계 | 그대로 |
| `190-h` | 조립후 전체계(하우징) | **신규 분리** |
| `190-i` | 조립후 전체계(인버터) | **신규 분리** |

**주의할 점**

- 이 분리는 **이 트렌드 앱 안에서만의 구분**이다. QR 정보관리 시스템(별도 저장소)의 `leak-pre-full.html`/`leak-post-full.html` 페이지 자체는 지금처럼 하우징·인버터를 한 페이지에서 같이 다룬다 — 바꾸지 않았다.
- `EQ_IDS`/`EQ` 객체는 `index.html` 안 `<script>` 태그, `var EQ = {` 로 시작하는 부분에 있다(현재 316번째 줄 — 코드가 바뀌면 줄 번호도 바뀌니 검색으로 찾을 것). 탭을 더 추가·수정하려면 여기를 고친다 — 자세한 편집 방법은 §3-2.
- 채널 키가 바뀌면(`130` → `130-h`/`130-i`) 구글시트의 "마스터샘플"·"기록" 시트에서 예전 `eq` 값(`130`)으로 쌓인 행은 새 채널과 연결되지 않는다. 이 변경 시점(2026-09-18)에는 실제 운영 데이터가 아직 없어(직전 테스트 데이터까지 전부 삭제 확인됨) 문제 없었다.

---

## 3-2. 설비 사양(상단 요약 카드) 수정 방법

화면 맨 위 네이비색 카드(공정명·설비명·디텍터 사양·테스트 압력·제품 판정 기준·관련 문서)는 데이터베이스가 아니라 **`index.html`에 직접 적힌 값**이다. 스펙(지정값·상하한, ④에서 입력)과는 완전히 별개다.

1. `index.html` 을 텍스트 편집기로 열어 `var EQ = {` 를 검색 (현재 316번째 줄)
2. 고칠 채널의 블록을 찾는다 — 키(`"120"`, `"130-h"` 등)로 구분되어 있다. 각 필드:
   - `tab` — 채널 탭에 뜨는 짧은 이름
   - `title` — 페이지 제목(`<h1>`)
   - `proc` — "공정명 / 공정No." 칸
   - `eq` / `det` / `pres` / `crit` — 설비명 / 디텍터 사양 / 테스트 압력 / 제품 판정 기준 칸
   - `doc` — 관련 문서 칸
   - `defaults` — 그 채널을 처음 열었을 때 자동으로 만들어지는 마스터 샘플 이름 목록 (이미 샘플이 있는 채널에서는 효과 없음 — ④에서 직접 추가·수정)
3. 저장 후 `git add`/`commit`/`push` — 수 분 내 실제 사이트에 반영된다

새 채널을 통째로 추가하려면 이 객체에 새 키를 하나 더 넣고, `EQ_IDS` 배열에도 그 키를 추가한다. 그러면 탭이 자동으로 하나 늘어난다.

---

## 3-3. 주간·월간 보기 전환 (2026-09-19 추가)

②그래프·③월별(주별) 기록이 "월간"과 "주간" 두 단위 중 골라 볼 수 있다. 채널 탭 밑, 기간 이동 바(‹라벨›) 위에 **주간/월간** 전환 버튼이 있다.

- **주는 월요일 시작 · 일요일 끝**으로 고정한다(공장 교대 근무 표기 관행에 맞춤). 예: 9월이면 9/7(월)~9/13(일).
- 전환·이동은 `period`("month" | "week") · `weekStart`(그 주의 월요일 날짜) 두 상태값으로 관리한다(`ym`은 월간 모드 때와 동일하게 계속 쓰임). 핵심 함수: `periodRecs()`(현재 보기 단위에 맞는 기록만 필터), `periodLabel()`/`periodTitle()`(짧은/긴 라벨), `mondayOf()`/`weekEndOf()`(날짜 계산).
- **미래로는 못 넘어간다** — 월간 모드가 이번 달 이후를 막는 것과 같은 규칙을, 주간 모드도 "이번 주 이후" 기준으로 그대로 적용한다(`nextM` 버튼 비활성화 조건에 분기 추가).
- ① 시업 전 점검 입력의 일자를 바꾸면, 주간 모드에서는 그 날짜가 속한 주로, 월간 모드에서는 그 날짜가 속한 달로 화면이 따라 이동한다(기존 월간 동작 그대로 확장).

**월 경계를 걸치는 주 (예: 9/28~10/4) 처리:** 이런 주는 구글시트에서 **두 달을 각각 받아와야** 정확하다 — `periodMonths()`가 그 주가 걸친 "YYYY-MM" 목록(1개 또는 2개)을 돌려주고, `syncNow()`가 그 목록 전부를 병렬로 pull한다. `applyPulled()`의 "이 응답이 아직 필요한가" 판정도 `ymAt === ym` 단순 비교에서 `periodMonths().indexOf(ymAt) >= 0`로 바뀌었다 — 안 그러면 주간 모드에서 두 번째 달 응답이 항상 무시된다.

**토글 버튼이 두 군데에 있다 (2026-09-19(6차) 추가):** 페이지 맨 위(`#periodSeg`, 채널 탭 바로 아래)와 **②그래프 카드 제목 바로 아래**(`#periodSeg2`) 두 곳에 같은 주간/월간 버튼이 있다. 맨 위 것만 있었을 때 "②그래프까지 스크롤해서 보면 토글이 화면 밖으로 벗어나 안 보인다"는 보고를 받아 카드 안에도 하나 더 뒀다. 두 토글은 `setPeriod()` 한 곳에서 함께 갱신되고(`document.querySelectorAll("#periodSeg button, #periodSeg2 button")`), 클릭 리스너도 둘 다에 걸려 있어 어느 쪽을 눌러도 같은 상태를 공유한다 — 새로 추가하는 컨트롤이 생기면 이 패턴(공통 갱신 함수 + 여러 DOM에 동시 반영)을 따르면 된다.

**이전/다음 이동 버튼도 같은 이유로 두 군데(2026-09-21 추가):** ‹ 라벨 › 이동 바(`#prevM`/`#nextM`/`#mLabel`/`#mMeta`)도 원래 페이지 맨 위에만 있어, 그래프를 주간 보기로 바꾼 뒤 다른 주로 이동하려면 맨 위까지 스크롤해야 했다. ②그래프 카드 안(주간/월간 토글 바로 아래)에 같은 이동 바를 하나 더 뒀다(`#monthBar2`, `#prevM2`/`#nextM2`/`#mLabel2`/`#mMeta2`) — `renderMonth()`가 라벨·안내문·다음 버튼 비활성 여부를 두 군데 모두에 함께 반영한다.

---

## 4. 접속 암호 (`gate.js`)

- **2026-09-19(5차)부터 `sessionStorage`(탭 단위)** — QR 정보관리 시스템과 동일하게 맞춤. 탭을 닫지 않는 한(같은 탭에서 새로고침해도) 다시 안 묻지만, QR을 다시 찍어 새 탭이 열리거나 앱을 완전히 종료했다 재실행하면 매번 다시 암호를 입력해야 한다.
- 이전(2026-09-18까지)에는 `localStorage`(기기 단위, 최초 1회)였다 — 명시적 요청으로 되돌린 것이며, 트레이드오프와 배경은 `개선이력.md`·`gate.js` 상단 주석 참고.
- **화면 문구는 이 지속 방식을 설명하지 않는다(2026-09-19(6차)):** 처음에는 "탭을 닫지 않는 동안에는 다시 안 묻는다"를 그대로 안내했는데, 재인증을 피하는 방법을 알려주는 셈이라 악용 소지가 있다는 지적을 받아 "계속하려면 접속 암호를 입력해 주세요."로 중립화했다. **실제 동작(sessionStorage)은 그대로**이고, 화면에 그 방식을 설명하지 않을 뿐이다.
- 비밀번호 입력값은 `.trim()`한 뒤 비교한다(2026-09-19(6차)) — 모바일 자동완성·자동교정이 앞뒤 공백을 붙이는 경우를 막기 위한 방어적 조치. 「기타사항」 해제 암호·관리자 암호 입력도 동일하게 trim한다.
- 2차 암호 단계 없음 (기술문서·PDF 뷰어가 없는 프로젝트라 불필요).
- 현재 암호: 기존 QR 정보관리 시스템과 **동일** (`PASS_HASH`는 `gate.js` 안). 바꾸려면 새 암호의 SHA-256 해시로 교체.

### 화면 구조 — 진짜 2단계(2026-09-19(7차) 재설계)

QR 정보관리 시스템(`intro.js` → `gate.js`)과 개념을 맞추기 위해, 배경 연출과 암호 입력을 **분리된 두 장면**으로 다시 짰다.

1. **1단계(항상 재생, 입력칸 없음)** — 격자·스캔선·꺾은선 그래프(지점 10개 + y축 눈금 숫자 + 단위 라벨, "계측기 화면"처럼 보이도록 디테일을 더함)가 그려지고, 그 위로 브랜드명·설비명("e-AWD 70kW 모터 하우징 / 마스터 누설값 관리 시스템")·밑줄이 순서대로 떠오른다. **인증 여부와 무관하게 매번 재생**된다(2026-09-19 결정, §위 참고).
2. 1단계가 끝나면: **이미 인증된 탭**은 2단계 없이 바로 콘텐츠로 넘어가고, **아직 인증 전인 탭**은 1단계 전용 요소(스캔선·그래프·문구)만 빠르게 사라지며 **2단계** = 순수 암호 카드(브랜드·제목·입력칸)가 새로 나타난다.
3. 타이밍은 QR 시스템의 `인트로_암호게이트_구현상세.md`와 맞췄다 — 1단계가 끝난 뒤 멈춰 있는 시간(`TAIL_MS` 1000ms)과 콘텐츠로 넘어가는 마지막 페이드(`FADE_MS` 1200ms)가 QR 시스템의 intro.js와 같은 값이다. 암호가 맞았을 때도 이제는 이 `FADE_MS`만큼 페이드아웃한 뒤 콘텐츠로 넘어간다(예전엔 즉시 전환).

**구현 중 자체 발견(중요) — `animationend` 카운터의 "한 번만 실행" 가드를 리팩터링 중 빠뜨려 암호 카드가 여러 장 겹쳐 쌓이는 회귀가 났었다.** `phase1Done` 플래그로 막았다. 이 카운터 방식(delay/duration을 나중에 바꿔도 안 깨지도록 "끝난 애니메이션 개수"를 세는 패턴, 4-2절 참고)으로 타이밍을 짜는 코드를 고칠 때는 이 가드가 왜 있는지부터 확인할 것 — 상세 경위는 개선이력.md(7차)[1] 참고.

---

## 4-1. 마스터 샘플 폐기 · 교체 (2026-09-18 추가)

마스터 샘플이 노후·손상돼 새 샘플로 바꿔야 할 때, **기존 방식인 "삭제"는 그 샘플로 쌓인 지난 기록·그래프까지 전부 지워버린다** (④의 "삭제" 버튼 — 지금도 그대로 있고, 순수 오타 등 진짜 잘못 만든 샘플을 지울 때는 이걸 쓴다). 샘플을 교체하는 상황에는 안 맞아서, **지난 이력은 남기고 새 샘플로 넘어가는 "폐기(교체)" 기능**을 따로 만들었다.

**사용법 (④ 마스터 샘플 · 스펙 관리)**

1. 교체할 샘플의 **[폐기(교체)]** 버튼 클릭 → 사용 중지 시점(날짜) 입력, 기본값은 오늘
2. 그 샘플은 `retired:true, retiredAt:"YYYY-MM-DD"` 로 표시되고, **①시업 전 점검 입력 화면에서 더 이상 나타나지 않는다** — 새로 값을 입력할 수 없게 됨
3. **②그래프·③월별 기록·CSV 내보내기에는 계속 나온다** — 폐기 이전에 쌓인 이력은 그대로 유지됨. 그래프 제목에 "폐기됨(YYYY-MM-DD)"이 함께 표시됨
4. **[+ 마스터 샘플 추가]** 로 새 샘플(예: "OK 마스터2")을 등록하면, 그 시점부터는 새 샘플로 기록이 쌓인다
5. 실수로 폐기했다면 **[사용 재개]** 버튼으로 즉시 되돌릴 수 있다 (`retired:false`)

**왜 그래프·기록 화면은 손대지 않았나:** ②그래프는 원래도 "그 달에 기록이 있는 샘플만" 그리므로, 폐기된 샘플도 기록이 있던 과거 달을 볼 때는 자동으로 나타나고 없는 달에는 안 나타난다 — 별도 분기 처리가 필요 없었다. ③월별 기록표도 마찬가지로 `cur().samples`(폐기 포함 전체)를 그대로 컬럼으로 쓴다.

**서버(구글시트) 반영:** `tools/apps-script/Code.gs`의 "마스터샘플" 시트에 **폐기여부·폐기일자 2개 열이 추가**됐다. 이미 구글시트 연동을 설정해 두었다면, 이 기능이 여러 폰에 정상 반영되려면 **Apps Script 재배포가 필요하다** (`구글시트_연동_설정방법.md` 맨 위 안내 참고). 재배포 전에는 폐기 처리가 그 폰에만 적용되고 다른 폰에는 안 보인다.

**QR 코드 번호 (2026-09-19 추가):** ④의 각 샘플에 "QR 코드 번호" 입력칸이 샘플명 옆에 생겼다(`s.qrNo`). 그 마스터 샘플(물리적 시편)에 실제로 붙여 둔 QR 라벨 번호를 적어 두는 용도 — 이 앱이 QR을 스캔해 들어가는 방식과는 별개로, **시편 자체를 식별하는 물리적 라벨 번호**를 기록해 두면 여러 개의 비슷한 샘플을 헷갈리지 않게 구분할 수 있다. 화면 표시·그래프에는 안 쓰고 ④ 관리 화면에만 있다.

---

## 4-3. 전체 이력 보기 (2026-09-17 추가)

②그래프·③월별 기록은 **모두 "그 달" 기준**이라, 한 샘플이 쓰인 전 기간을 보려면 달을 하나씩 넘겨 가며 봐야 했다. 특히 폐기한 샘플의 지난 이력을 확인할 때 불편해서, **달 구분 없이 한 샘플의 전 기간을 한 화면에 띄우는 기능**을 넣었다.

- 위치: **④ 마스터 샘플 · 스펙 관리** → 각 샘플의 **[전체 이력 보기]** 버튼 (기록이 한 건이라도 있는 샘플에만 나온다)
- 내용: 기간·총 기록 수·NG 건수·평균/최소/최대 요약 + 전 기간 꺾은선 그래프 + 전 기간 기록표(최신순)
- 폐기된 샘플도 그대로 볼 수 있다 (창 위에 폐기일 안내가 뜬다)

**주의 — 왜 서버에서 다시 받아오는가:** 평소 화면은 **이번 달치만** 서버에서 내려받는다(`syncNow`). 그래서 "이 폰에 저장된 것"만 가지고 전체 이력을 그리면 **다른 폰에서 입력한 지난 달 기록이 빠진다.** 그래서 이 창을 열 때 `pull`을 **달 지정 없이** 한 번 더 호출해 전 기간을 받아온다 (`Code.gs`의 `pull`은 `ym`이 비어 있으면 그 설비의 모든 달을 돌려준다).

받아오지 못한 경우(구글시트 연동 미설정·통신 실패·미전송 건이 남은 경우)에는 창 맨 위에 **"이 폰에 저장된 기록만 보이고 있습니다"** 경고가 뜬다. 이 경고가 보이면 이력이 완전하지 않다는 뜻이므로, 통신이 되는 곳에서 다시 열어 봐야 한다.

---

## 4-4. 그래프 읽기 · 덮어쓰기 확인 (2026-09-17 추가)

**그래프 눈금:** 가로·세로축 모두 **주 눈금(값이 적힌 진한 선) + 보조선(그 사이 옅은 선)** 구조다. 세로축 눈금 간격은 값 범위에 맞춰 1·2·5×10ⁿ 중에서 자동으로 고르므로(`niceStep`), 3.33 같은 어중간한 눈금값이 나오지 않는다. **가로축(2026-09-21 추가):** 데이터 점마다 X축 기준선 위에 짧은 눈금(ruler tick)을 찍어, 축만 봐도 기록 횟수가 드러나게 했다. 주간 보기일 때는 라벨에 요일도 같이 붙는다(`weekdayKo()`, 예: "21(월)") — 월간 보기는 점이 많아 요일까지 넣으면 빽빽해지므로 그대로 둔다.

**꺾은선 면적 채우기 (2026-09-21 추가):** 값 선 아래에 옅은 그라디언트 면적을 깔아(게이트 화면의 장식 그래프와 같은 방식) "채워진 그래프"처럼 보이게 했다. `buildChart()`는 한 화면에서 샘플마다 여러 번 호출되므로, `<linearGradient>` id를 `"chartFill-" + s.id`로 샘플마다 고유하게 만든다 — 모든 SVG가 같은 id를 쓰면 문서 전체에서 첫 번째 정의만 유효해(중복 id 무효) 다른 샘플의 채우기 색이 뒤섞일 수 있다.

**점 터치:** 그래프의 점을 손가락으로 누르면 **그때의 누설값·판정·일자·교대**가 말풍선으로 뜨고 2초 뒤 사라진다(같은 점을 다시 누르면 바로 닫힘). 점이 촘촘해 정확히 누르기 어려우므로, 누른 가로 위치에서 **가장 가까운 점**을 골라 준다. 이미지로 저장할 때는 강조 표시가 함께 찍히지 않도록 지운 뒤 저장한다.

**덮어쓰기 확인:** ①에서 **이미 기록이 있는 일자·교대**에 저장하면(버튼이 "수정 저장"으로 바뀐 상태), **"지금 저장된 값"을 보여 주는 확인창**이 한 번 뜬다. 작업자가 실수로 눌러 기존 기록을 날리는 것을 막기 위한 것이다. **새로 입력하는 경우에는 확인창이 뜨지 않는다** — 잘못 눌러도 잃을 기록이 없고, 매번 물으면 매일 하는 입력이 번거로워지기 때문이다.

- **2026-09-19(5차):** 확인창 말고도 **폼 안의 저장 버튼 위 안내 문구**(`saveHint`)로도 같은 사실을 알려 주는데, 원래는 다른 문구들과 똑같은 회색 톤이라 눈에 잘 안 띈다는 지적을 받았다. 덮어쓰는 상황일 때만 `.hint-warn` 클래스를 붙여 **적색 굵은 글씨 + 옅은 적색 배경 박스**로 강조한다. 새 일자·교대를 고르면 클래스가 자동으로 빠지고 원래 문구로 돌아간다.

**NG 점 강조 (2026-09-19(5차) 추가, (6차)에서 상시 표시로 변경):** 그래프의 점 중 판정이 NG인 점은 렌더링될 때 `class="pt-ng"`가 붙고, 붉게 흐려졌다 진해지는 것을 반복하는 펄스 애니메이션(`ngPulse`)이 **항상** 걸린다. 처음(5차)에는 PC에서 마우스를 올려야만 뜨는 `:hover` 전용 효과였는데(터치 기기의 sticky-hover 부작용을 피하려는 의도), "마우스가 안 가도 보이게 해 달라"는 요청으로 (6차) `@media (hover:hover) and (pointer:fine)` 가드와 `:hover` 선택자를 없애고 상시 애니메이션으로 바꿨다 — 더 이상 hover 상태에 의존하지 않으므로 sticky-hover 부작용도 함께 사라졌다. `prefers-reduced-motion`(동작 줄이기) 환경에서는 이 펄스 애니메이션이 꺼진다.

---

## 4-2. 화면 디자인 — 색상 테마 · 게이트 애니메이션 (2026-09-18 추가)

**색상 테마:** 기존 네이비+시안 계열에서, 따뜻한 크림/베이지 바탕 + 다크 틸(진한 청록) 포인트 색상으로 전체 교체했다 (사용자가 제공한 "Neo-Skeuomorphic Tactile System" 참고 이미지 기준). 이 프로젝트(누설값 관리 시스템)에만 적용되며, **별개 프로젝트인 QR 정보관리 시스템의 색상은 그대로**다 — 두 프로젝트는 `styles.css` 파일명은 같아도 서로 다른 저장소라 독립적이다.

- 기본 팔레트는 `styles.css`의 `:root` 변수로 정의 (`--navy`, `--accent`, `--gray-*`, `--ok-bg`/`--warn-bg` 등) — 여기 값만 바꾸면 카드·버튼·표 등 대부분 색상이 한 번에 바뀐다.
- 다만 `index.html`(그래프 SVG를 JS로 그리는 부분, 뱃지·버튼 일부 인라인 스타일)과 `gate.js`(암호 화면은 `<style>` 문자열을 JS로 주입하는 구조라 CSS 변수를 못 씀)에는 직접 적은 hex 값이 남아 있어, 이번에 같은 톤으로 손수 맞춰 바꿨다. **나중에 색을 또 바꾸게 되면 이 두 파일도 같이 찾아봐야 한다** — `styles.css`의 `:root`만 고치고 끝나지 않는다.
- 경고/위험 색(주황·빨강 계열)은 크림 배경에서도 원래 톤이 잘 어울려서 그대로 뒀다.
- **①~⑥ 섹션 색 구분 (2026-09-21 추가):** `styles.css`의 `.section .num`은 원래 전 섹션이 같은 남색이라, 스크롤 중에 지금 몇 번 섹션인지 한눈에 안 들어온다는 지적을 받았다. `styles.css`는 QR 정보관리 시스템과 공유하는 파일이라 거기서 고치지 않고, `index.html`에서 섹션 id별로 번호 배지 + 카드 위쪽 3px 띠 색을 덮어쓴다: ①남색 `#1b4a49` ②틸 `#2f6e6c`(그래프 선과 동일) ③앰버 `#a8752f`(추세선과 동일) ④플럼 `#7d5b8f` ⑤러스트 `#9c5a3c` ⑥슬레이트 `#4a6670`.

**게이트(암호 입력) 화면 애니메이션:** 처음 제시했던 5개 안 중 "③꺾은선 그리기"와 "④격자 스캔"을 합쳤다. 순서: 계측 그리드가 옅게 깔림 → 스캔 라인이 위에서 아래로 한 번 훑음 → 그 뒤로 누설값 꺾은선 그래프가 그려짐 → 마지막에 카드가 떠오름. `prefers-reduced-motion`이면 애니메이션 없이 바로 다 보이게 처리됨. 코드는 `gate.js` 안에 전부 있다(인증 로직은 그대로, 배경 연출만 추가).

> **2026-09-19(7차) 업데이트:** 아래는 애니메이션 자체의 도입·튜닝 이력(그대로 유효)이지만, **화면이 몇 개의 장면으로 나뉘는가**는 이후 2단계 구조로 다시 짜였다 — 최신 구조는 §4 "화면 구조 — 진짜 2단계" 참고. 이 절의 "카드"는 지금은 1단계(브랜드·설비명, 입력칸 없음)와 2단계(순수 암호 카드)로 나뉘어 있다.

- **2026-09-19:** 처음에는 기기당 최초 1회만 재생하고 이후로는 애니메이션 없이 즉시 화면이 떴는데, QR을 찍을 때마다 이 연출을 보고 싶다는 요청으로 **매번 재생**하도록 바꿨다. 다만 **암호를 다시 묻지는 않는다**:
  - 처음 통과하는 폰: 예전과 같음 — 연출 뒤에 암호 카드(입력칸 있음)가 뜬다.
  - 이미 통과한 폰(교대 작업자가 매일 쓰는 폰 등): 같은 연출이 재생되는 동안 입력칸 없는 브랜드 카드만 보여 주고, 연출이 끝나면(약 2초) 화면이 페이드아웃되며 암호 입력 없이 바로 앱으로 넘어간다.
  - **`prefers-reduced-motion`(동작 줄이기)이 켜진 기기에서도 이 화면은 똑같이 뜬다.** 처음에는 "동작 줄이기가 켜졌고 이미 통과한 기기면 연출을 건너뛴다"는 예외를 뒀는데, 실제 현장 폰 중 그 설정이 켜진 기기에서 **연출도 카드도 아무것도 안 뜨는 것처럼 보이는 문제**가 생겨 예외를 없앴다. 지금은 CSS의 `prefers-reduced-motion` 미디어쿼리가 격자·스캔·선·카드의 **전환 애니메이션만** 정적으로 바꿀 뿐, 화면 자체는 항상 표시된다.
  - 참고: 폰에 "동작 줄이기"가 켜져 있는지는 사용자가 의식하지 못하는 경우가 많다(배터리 절약 모드에서 자동으로 켜지기도 함). 그래서 이 설정을 기준으로 화면을 통째로 건너뛰는 분기는 두지 않는 편이 안전하다.

- **2026-09-19 (2차):** "너무 빠르다"는 피드백으로 속도를 다시 잡았다. QR 정보관리 시스템의 [`인트로_암호게이트_구현상세.md`](../후공정%20설비%20QR%20코드%20스마트폰%20관리%20시스템/후공정%20설비%20정보%20시스템%20프로젝트/인트로_암호게이트_구현상세.md) §3.4·§3.5를 참고해 이식했다.
  - **핵심: 체감 속도는 `TAIL_MS`가 결정한다.** 등장 시간(duration)만 늘리면 "또렷해지는 시점"이 뒤로 밀릴 뿐, **또렷한 상태로 멈춰 있는 시간**은 그대로라 체감이 안 바뀐다. 그래서 `gate.js` 맨 위에 `TAIL_MS`(연출이 다 끝난 뒤 멈춰 있는 시간) / `FADE_MS`(페이드아웃) / `TIMEOUT_MS`(안전 종료) 3개 상수를 두고 여기서 조절하게 했다. **속도를 또 바꾸려면 `TAIL_MS`부터 만질 것.**
  - 현재 값: `TAIL_MS` 900ms · `FADE_MS` 700ms. 마지막 애니메이션(카드)이 1,800ms에 끝나므로 **전체 약 3.4초**(이전 2.0초), 글자가 또렷한 상태로 머무는 구간은 약 1.6초(이전 0.5초).
  - **고정 타이머를 쓰지 않는다.** 예전에는 `setTimeout(..., 1600)`으로 종료 시점을 고정했는데, 최초 로드 때 애니메이션 시작이 수백 ms 밀리면 마지막 동작이 잘리고, delay·duration을 조정하면 "무엇이 가장 늦게 끝나는지"가 바뀌어 또 깨진다. 지금은 `animationend`가 몇 번 왔는지 **개수를 세서** 전부 끝난 뒤 `TAIL_MS`만큼 쉰다.
  - 안전장치 2개: ① `TIMEOUT_MS`(6초) — 백그라운드 탭에서는 `animationend`가 아예 안 올 수 있어 필요하다. ② 동작 줄이기 기기는 CSS가 애니메이션을 꺼서 역시 `animationend`가 안 오므로, 이 경우에만 시간으로 잰다(화면을 건너뛴다는 뜻이 아니다 — 위 항목 참고).
  - 암호 입력칸 자동 포커스를 카드가 떠오른 뒤(1,850ms)로 미뤘다. 연출 중에 포커스하면 폰에서 키보드가 먼저 올라와 연출을 가린다.
  - 접근성 속성(`role="dialog"`, `aria-modal`, `aria-labelledby`, 입력칸 `aria-label`, 오류 문구 `role="alert"`)을 QR 시스템과 같은 수준으로 맞췄다.

- **2026-09-19 (3차) — 글자 가독성:** 꺾은선 SVG(`.bg-line`)가 원래 화면 한가운데(`top:38%~62%`)를 지나가 카드 글자와 정확히 겹쳐, 배경 격자·스캔선 위에서 글자가 잘 안 보였다. 두 가지로 고쳤다: ① 꺾은선을 카드 아래쪽(`top:68%~86%`, 불투명도 .55)으로 내려 글자와 겹치지 않게 했다. ② 카드 자리에 타원형 그라디언트 음영(`.bg-scrim`, 중심부는 배경색에 가깝게 진하고 가장자리로 갈수록 투명)을 깔아, 격자·스캔선이 그 위를 지나가도 글자는 항상 또렷하게 보이게 했다. 동작 줄이기(`prefers-reduced-motion`) 환경에서는 음영도 `opacity:1`로 즉시 켜 둬야 한다 — 기본값이 0이라 빼먹으면 그 환경에서만 음영이 안 보여 다시 안 읽히는 문제가 생긴다(구현 중 자체 발견, 개선이력.md 참고).

- **2026-09-19(5차) — PC 화면에서 꺾은선이 왼쪽으로 쏠림:** `.bg-line`(꺾은선 SVG)에 `width`를 지정하지 않고 `left`/`right`로만 폭을 정해 뒀는데, `<svg>`처럼 고유 비율(`viewBox`)이 있는 "교체 요소"는 `width`가 없으면 `left`/`right`를 무시하고 `viewBox` 비율과 `height`만으로 폭을 역산한다. 화면이 좁을 때는 차이가 작아 눈에 안 띄었지만, PC처럼 넓은 화면일수록 실제 폭이 의도보다 훨씬 좁아져 왼쪽에 쏠려 보였다. `width:84%`를 명시해 고쳤다.

- **2026-09-19(6차) — 격자·그래프 디테일 보강:** 격자 선 색을 배경과 가까운 톤(`#2c5f5d`)에서 더 밝은 톤(`#3d7a78`)으로 올리고, 기존 촘촘한 보조선(28px 간격, `.bg-grid`) 위에 굵은 주 눈금 한 겹(112px 간격, `#4a8f8c`, `.bg-grid-major`)을 더 겹쳤다. 꺾은선에는 옅은 점선 기준선 1개, 선 아래 그라디언트 면적 채우기(`.bg-line-area`), 각 지점의 작은 점(`.bg-line-dots`, 마지막 지점은 더 크고 밝게)을 추가해 실제 트렌드 그래프에 가까운 인상을 준다. 이미 통과한 폰에서 연출 종료 시점을 세는 `animated` 요소 목록에도 새로 추가한 요소들을 반드시 같이 넣어야 한다 — 안 넣으면 `animationend` 카운트가 실제보다 적게 잡혀 새 애니메이션이 덜 끝난 상태에서 화면이 넘어가 버린다(구현 중 자체 발견). 이 작업 중 재확인하다가 `prefers-reduced-motion`에서 꺾은선이 `stroke-dashoffset` 값 때문에 아예 안 그려질 수 있던 기존 결함도 같이 발견해 고쳤다(개선이력.md [6차][3] 참고).

**`<noscript>` 방어 (2026-09-19 추가):** 게이트가 JS로 화면을 가리는 방식이라 **브라우저에서 자바스크립트를 끄면 게이트가 통째로 무력화**된다. `index.html`의 `<body>` 앞부분에 `<noscript>`로 본문을 숨기고 안내 문구를 띄우는 블록을 넣었다(QR 시스템과 동일 방식). 완전한 차단은 아니지만, 이게 없으면 JS만 꺼도 내용이 다 보인다.

- **2026-09-19(8차) — 그래프 더 진하게, 문구를 그래프와 같이 등장시킴:** 그래프 전체 밝기(`.bg-line`의 `opacity`)를 `.55`→`.75`로, 점선 기준선·실선·y축 눈금 숫자·단위 라벨도 각각 더 진하게(굵기·opacity 상향) 조정했다. 브랜드·설비명 문구는 원래 그래프가 다 그려진 2초 뒤에야 나타났는데, **꺾은선이 그려지기 시작하는 시점(0.5~0.6초)에 맞춰 같이 나오도록** 앞당기고, 문구가 나타나는 데 걸리는 시간(등장 애니메이션 길이)도 늘렸다(브랜드 1.1s→1.6s, 설비명 1.2s→1.8s, 밑줄 0.7s→1s) — "그래프 등장할 때 같이 문구도 나오게 하고, 그 등장 타임을 조금 더 길게" 요청.

---

## 4-5. 추이 이상 신호 자동 탐지 (2026-09-19 추가)

②그래프(월별·전체 이력 둘 다)에, 스펙(상·하한) 이탈과는 별개로 **"이 샘플의 평소 패턴과 통계적으로 다른 움직임"**을 자동으로 잡아 경고 상자로 보여주는 기능. **AI가 아니라 통계 계산**이다 — 코드는 `index.html`의 `detectTrend()`(제조업에서 흔히 쓰는 "통계적 공정관리(SPC)" 규칙 4개, Nelson Rules 일부를 참고) · `renderTrendNote()`.

**왜 AI로 안 만들었나:** 사용자에게 설명한 대로, 무료로 쓸 수 있는 AI가 없고(모델 호스팅·API 호출 모두 비용 발생), 설비 1개소당 월 수십 건 수준의 데이터로는 "학습"이 안 되며, 애초에 "이 드리프트는 어떤 원인 때문이었다"는 정답 기록 자체가 없어 학습시킬 데이터가 없다. 통계 규칙은 무료이고, 왜 신호로 잡혔는지 항상 설명 가능하며, 같은 데이터에는 항상 같은 결과를 낸다.

**탐지 규칙 4가지** (모두 "지금 이 순간 진행 중인" 신호만 본다 — 과거에 있다 사라진 건 대상 아님):

| 규칙 | 조건 | 최소 표본 | 의미 |
|---|---|---|---|
| 연속 추세 | 최근 값이 6회 연속 계속 커지거나 계속 작아짐 | 6개 | 서서히 나빠지는 중일 수 있음 |
| 평균 쏠림 | 최근 값이 8회 연속 이 샘플 평균의 한쪽에만 있음 | 8개(+그 전 데이터) | 뭔가 바뀌어 수준 자체가 이동함 |
| 경고 구간 근접 | 최근 3회 중 2회 이상이 "그 이전까지의" 평균 ±2σ 바깥 | 8개 | 스펙 안이지만 평소보다 불안정 |
| 변동성 급증 | 최근 5회의 표준편차가 그 이전 구간의 1.8배 초과 | 10개 | 들쭉날쭉해짐 |

값이 이보다 적은 달에는 판단하지 않는다(표본이 적을 때 억지로 판단하면 오탐이 늘어나므로).

**"경고 구간 근접" 규칙의 설계 포인트:** 평균·표준편차를 **판정 대상(최근 3개)을 뺀 나머지**로만 계산한다. 처음에는 전체 값으로 계산했다가, 극단값 2개가 껴 있으면 그 극단값 자체가 평균·표준편차를 넓혀 버려서 **정작 그 극단값을 못 잡는 역설**이 생기는 걸 발견해 고쳤다(개선이력.md 참고).

**표현 방식 (2026-09-19 수정):** 처음에는 "표준편차 1.757" 같은 통계 용어·숫자를 그대로 문장에 넣었는데, 현장 작업자가 이해하기 어렵다는 피드백을 받고 두 단으로 나눴다.
- **`plain`** — 무엇이 일어나고 있고 왜 신경 써야 하는지를 통계 용어 없이 풀어 쓴 문장. 경고 상자에서 굵고 크게 보이는 주 텍스트.
- **`detail`** — 날짜·수치 등 검증용 근거. "근거: " 접두어를 붙여 작게, 흐린 색으로 보조 표시.

변동성 급증 규칙은 배율("이전보다 약 X배")로 표현하는데, **직전 구간이 우연히 거의 완벽하게 평평했다면 배율이 70배처럼 비현실적으로 커질 수 있다**(계산은 맞지만 그대로 보여주면 신뢰가 떨어짐). 그래서 5배 이상이면 정확한 숫자 대신 "5배 이상으로"라고 뭉뚱그려 표현한다.

**부동소수점 오차 처리:** `mean()`으로 계산한 평균은 `3.2`가 정확히 `3.2`로 안 나올 수 있다(예: `3.1999999999999997`) — 완전히 안정적인 값(예: 계속 3.2)인데도 "평균보다 미세하게 높다"로 잘못 갈릴 뻔한 걸 구현 중 자체 검증으로 발견해, `TREND_EPS`(1e-9) 여유를 두어 막았다. 실제 측정 정밀도(소수 3자리, 0.001 단위)보다 한참 작은 값이라 진짜 차이는 그대로 잡힌다.

**패턴별 "흔한 원인" 목록(`CAUSE_HINTS`):** 일반적인 리크테스트 품질관리 지식으로 만든 **초안**이다. 실제 현장 경험과 다르면 `index.html`에서 `CAUSE_HINTS` 객체를 찾아 문구를 직접 수정하면 된다 — 코드 구조를 몰라도 한글 문자열만 바꾸면 되게 만들어 뒀다.

**"근거" 줄의 통계 용어 명시 (2026-09-19):** "표준편차 1.757" 처럼 라벨 없이 숫자만 나오면 "이게 무슨 값인지 모르겠다"는 피드백을 받았다. 지금은 "경고 구간 근접"·"변동성 급증" 두 규칙의 근거 문장에서 **"표준편차"라는 용어를 명시하고, 그 옆에 괄호로 짧게 정의**("값이 평소 얼마나 흩어져 있는지 나타내는 통계 수치")를 붙인다. "경고 구간 근접"은 실제 경계값(예: "위쪽 경계는 3.296 mL/min")도 함께 보여줘, 추상적인 "2배" 대신 구체적인 숫자로 확인할 수 있게 했다.

**②그래프 카드의 분석은 "지금 보이는 기간"과 무관하게 전체 기록 기준(2026-09-19(7차)):** 위 규칙들은 최소 6~10개 데이터가 필요한데, 그래프를 주간 보기(§3-3)로 두면 한 주 데이터가 딱 6~7개뿐이라 사실상 항상 "신호 없음"이 나왔다. 그래프 카드에 넣는 분석은 지금 화면의 기간(주간/월간)과 무관하게 **그 샘플의 전체 기록**(`sampleHistory(s)`)으로 계산하도록 바꿨다 — 그래프 자체(점·선)는 그대로 지금 보이는 기간만 그린다. 그래서 분석에 인용된 날짜가 지금 화면의 기간 밖일 수 있고, 안내 문구에 그 점을 명시해 둔다.

### 추세선 · 기울기 · R² (2026-09-19(8차) 추가)

위의 추이 이상 신호가 "전체 기록 기준"으로 바뀌면서(7차), 그래프 카드 안에 **"지금 보이는 기간에 대한" 분석이 하나도 안 남는 부작용**이 생겼다 — 주간·월간을 바꿔도 카드 내용이 똑같아 보이는 문제로 이어졌다. 그래서 **그 카드에 지금 그려진 points(주간이면 그 주, 월간이면 그 달)만으로 계산하는** 별도의 선형회귀 추세선을 추가했다.

- `linreg(ys)`(최소제곱법) — x는 날짜 간격이 아니라 **그래프에 찍히는 순서**(0,1,2…)다. `buildChart()`의 X() 함수 자체가 순서 기준으로 점을 배치하므로, 추세선도 같은 기준이어야 그래프 위 직선이 어긋나지 않는다. 그래서 기울기 단위는 "측정 1회당"이지 "하루당"이 아니다(교대별로 하루 1~2회 기록될 수 있어 서로 다르다).
- 그래프에 주황색(`#a8752f`) 점선으로 그려지고, 그 아래 `.ch-regress`에 "측정 1회당 X mL/min · R² Y(직선에 가까운 정도: 약함/보통/뚜렷함)"를 표시한다(`regressionStatsHtml()`). 점이 2개뿐이면 회귀가 항상 완벽히 들어맞아 의미가 없어 **3개 이상일 때만** 그린다.
- 추이 이상 신호(전체 기록 기준, SPC 규칙)와는 역할이 다르다 — 추세선은 "지금 보이는 기간의 흐름", 추이 이상 신호는 "이 샘플 역사 전체와 비교한 이상 여부". 두 분석을 하나로 합치지 않고 나란히 둔 것은 의도적인 선택이다(개선이력.md 8차[3] 참고).
- `buildChart()`를 쓰는 곳(②그래프 카드, ④"전체 이력 보기" 모달) 모두에 공통 적용된다 — 전체 이력 모달에서는 전체 기간 전체를 하나의 회귀로 보여준다.

---

## 4-6. 도움말 위젯 (2026-09-19 추가)

화면 우측 하단에 항상 떠 있는 `?` 버튼(스크롤해도 같은 위치 유지 — `position:fixed`). 누르면 검색창 + 자주 묻는 질문 목록이 뜬다. 코드는 `index.html`의 `FAQ` 배열 · `searchFaq()` · `renderHelpList()` · `initHelp()`.

**자연어로 뭐든 답하는 AI 챗봇이 아니다.** 미리 써 둔 질문·답변 중 검색어와 겹치는 것만 보여주는 키워드 검색이다. 무료이고, 답이 항상 정확하다(모델이 지어내는 말이 없음)는 것이 트레이드오프 — §4-5(추이 이상 탐지)를 "AI 대신 통계"로 만든 것과 같은 이유다.

- **"가는 경로까지 인도":** 답변에 관련 섹션이 있으면 [여기로 이동] 버튼이 붙는다. 누르면 그 섹션(`<details>`)을 펼치고 그 위치로 스크롤한 뒤 위젯을 닫는다.
- **작업자용·관리자용 질문을 한 목록에 같이 둔다** (요청에 따른 설계). 관리자용 질문에는 주황색 "관리자" 표시를 붙여 구분한다.
- **질문 추가 방법:** `FAQ` 배열에 `{ q: "질문", keywords: "검색어들", a: "답변", goto: "섹션id"(선택), admin: true(선택) }` 한 줄만 추가하면 된다. 검색은 질문+키워드+답변 전체에서 부분 일치로 찾는다.
- z-index는 300 — 전체 이력 보기 모달(200)보다는 위, 저장/삭제 확인 토스트(9000)와 게이트 화면(99999)보다는 아래로 두어 서로 가리지 않게 했다.
- 현재 18개 문항(작업자용 10개, 관리자용 8개). 실제로 자주 나오는 질문이 생기면 이 배열에 계속 추가해 나가는 것을 권장한다.
- **화면 사용법에만 머물지 않는다(2026-09-19(8차)):** "표준편차가 뭔가요?", "마스터 NG가 반복해서 뜨면 어떻게 하나요?", "보정값(Comp Value)이 뭔가요?" 3건은 리크 디텍터(LS-R700/R902) 매뉴얼의 실제 내용(문제해결 챕터의 "Master NG 다발 시" 확인 순서, "Fixed Comp Settings" 설명)을 근거로 넣었다 — 도움말이 "이 화면은 이렇게 씁니다" 수준에만 머물면 실사용에서 큰 도움이 안 된다는 지적을 받고 보강했다. 새 FAQ를 매뉴얼·현장 자료에서 가져올 때는, 문서의 원문을 그대로 붙이지 말고 이 앱의 화면·기능과 연결해 풀어 쓸 것.

**검색 방식 개선 (2026-09-19):** 처음에는 "입력한 문장 전체가 FAQ 안에 그대로 들어있는가"만 봤는데, 그러면 "OK 마스터 트렌드 분석해줘"처럼 자연스러운 문장은 그 문장 그대로를 담은 FAQ가 없는 한 항상 0건이 나왔다(실사용 중 발견). 지금은 검색어를 단어 단위로 쪼개, FAQ의 질문+키워드+답변과 겹치는 단어 수로 점수를 매겨 정렬한다(`searchFaq()`).

**실시간 분석 (2026-09-19 추가):** 검색어에 "트렌드"·"분석"·"이상"·"신호" 같은 분석 의도 단어와 **현재 채널에 등록된 샘플명**이 함께 들어 있으면(예: "OK 마스터 트렌드 분석해줘"), FAQ를 찾아 주는 대신 그 샘플의 실제 데이터로 §4-5의 추이 탐지를 그 자리에서 돌려 진짜 결과를 보여준다(`matchLiveAnalysis()` · `renderLiveAnalysis()`). 이미 구현된 통계 계산을 재사용하는 것뿐이라 추가 비용은 없다 — "무엇이든 답하는 AI"가 아니라 "미리 만든 계산을 사용자 표현에 맞춰 실행해 주는 것"이라는 점은 여전히 같다.

- **매칭 정규화 (2026-09-19(5차)):** 위 문장 단위 점수 매기기와 별개로, `matchLiveAnalysis()`의 **샘플명 대조**는 원래 원문 그대로("OK 마스터")로 부분 일치를 봤다. 그래서 "OK마스터샘플 트렌드 분석을 해줘"처럼 **띄어쓰기가 다른 실제 입력**이 매칭에 실패하는 문제가 있었다(사용자 실사용 재현 보고). 검색어·샘플명 양쪽에 공백 제거 + 소문자 변환(`normForMatch()`)을 먼저 적용한 뒤 대조하도록 고쳐, 띄어쓰기·대소문자 차이와 무관하게 매칭되게 했다.

**모바일 키보드 가림 방지 (2026-09-19(5차) 추가, (6차)에서 수정):** 폰에서 위젯을 열고 검색칸을 누르면 화면 키보드가 올라오는데, 위젯이 원래 위치(화면 하단 고정)에 그대로 있으면 **키보드가 위젯 자체를 가려 안 보이는** 문제가 있었다. 터치 포인터 기기(`matchMedia("(pointer:coarse)")`)에서만 검색칸에 포커스가 가면 위젯에 `.kb-avoid` 클래스를 붙여 화면 위쪽으로 옮긴다(`initHelp()`). PC는 키보드가 화면을 가리지 않으므로 이 분기를 타지 않는다.

- **(6차) blur에서 위치를 되돌리지 않도록 수정.** 원래는 검색칸에서 포커스가 빠지면(`blur`) 곧바로 원래 위치로 되돌렸는데, 폰에서 검색어를 입력하고 키보드의 "검색/완료"를 누르는 것 자체가 blur를 유발하는 경우가 많아, **실시간 분석 결과가 막 만들어진 순간 패널이 사용자가 보던 화면 위쪽에서 아래로 튕겨 내려가 버렸다** — "엔터를 쳐도 반응이 없다"는 보고의 유력한 원인으로 판단했다. 지금은 패널을 닫을 때(`closeHelp`)만 위치를 원래대로 되돌리고, 열려 있는 동안은(포커스가 빠지더라도) 옮겨진 위치를 유지한다.

**정확히 일치하는 게 없으면 유사 질문을 추천 (2026-09-19(7차) 추가):** 검색어가 FAQ 문구와 겹치는 단어가 하나도 없으면(오타, 낯선 표현 등) 그냥 "못 찾았습니다"만 뜨는 게 챗봇이 반응 없는 것처럼 보인다는 지적을 받았다. `searchFaq()`가 0건이면 2글자 단위(bigram) 유사도로 FAQ 질문·키워드와 겹치는 정도를 계산해 가장 비슷한 질문을 최대 4개까지 "정확히 일치하는 답은 못 찾았지만, 혹시 이런 게 궁금하신가요?"로 보여준다(`fuzzySearchFaq()`). 유사도는 **검색어 쪽 bigram 수 기준**(`hits / 검색어 bigram 수`)으로 계산한다 — 처음에는 `max(검색어 길이, 대상 길이)`로 나눴다가, FAQ의 `keywords`가 길수록 대상 쪽 길이가 늘어나 점수가 부당하게 낮아지는 문제를 겪었다(개선이력.md 7차 참고). 임계값 0.3 미만이면 추천하지 않는다(관련 없는 질문이 섞이는 것 방지).

---

## 4-6-1. 맨 위로 이동 버튼 (2026-09-19(7차) 추가)

화면을 400px 이상 내리면 우측 하단에 나타나는 버튼(`#toTopBtn`). 도움말 위젯 버튼(`#helpFab`, z-index 300) 바로 위에 쌓아(z-index 290) 서로 겹치지 않는다. 누르면 `scrollTo({top:0, behavior:'smooth'})`로 맨 위로 이동하고, `prefers-reduced-motion`이면 즉시 이동한다(`behavior:'auto'`). 코드는 `initToTop()`.

---

## 4-7. 디텍터 보정 이력 (2026-09-19, QR 정보관리 시스템에서 이관)

리크 테스터 디텍터(LS-R700)를 마스터 샘플로 점검하고 Fixed comp value(보정계수)를 고친 이력을 기록하는 새 섹션(**⑤디텍터 보정 이력**, `secCalib`). 원래 QR 정보관리 시스템의 설비 상세 페이지 "⑨ 마스터 샘플·보정 이력" 아코디언에 있었으나, 그 시스템은 정적 페이지라 계속 값을 누적 입력할 방법이 없어 **이 시스템으로 이관**했다. 원본 인계 문서는 그 저장소의 `인계_마스터샘플_보정이력.md`(이관 시점 기준 원본 위치·데이터 구조 기록).

이 기능이 새로 생기면서 **기존 ⑤데이터 · 연동 설정은 ⑥으로 밀렸다.**

### 원본 대비 개선한 점

인계 문서 내용을 그대로 옮기지 않고, 이 시스템의 기존 구조에 맞춰 다음을 개선했다.

1. **채널 분리 활용** — 원본은 "OK 마스터(하우징)"/"OK 마스터(인버터)"처럼 샘플명에 채널을 접미사로 붙였다(§3-1 이전, 채널이 나뉘어 있지 않았을 때 설계). 지금은 130-h/130-i처럼 채널 자체가 나뉘어 있어 접미사가 필요 없다 — 그냥 그 채널의 ④에서 관리하는 마스터 샘플을 **드롭다운으로 선택**한다(오타 방지, 자유 텍스트 아님).
2. **판정 자동 제안** — 원본은 판정(OK/NG)을 완전 수동 입력했다. 지금은 리크값-후를 입력하면 **선택한 샘플의 스펙(하한·상한)과 비교해 자동으로 제안**한다(`judge()` 재사용). 다만 강제는 아니다 — 드롭다운이라 언제든 손으로 바꿀 수 있다. 이 자동 제안이 항상 옳다고 볼 수는 없다는 점은 유의 — 캘리브레이션 판정 기준이 그 샘플의 일반 스펙과 다를 수 있는 경우, 실제 역사 데이터(§ 아래 표)에서도 자동 제안과 원래 판정이 다르게 나올 수 있다.
3. **CSV 내보내기 추가** — 원본에는 없던 기능. ⑤에서 [보정 이력 CSV 내보내기]로 받을 수 있다(기존 CSV와 같은 수식 삽입 방지 처리 포함).
4. **접근 제한을 구체적인 메커니즘으로 구현** — 인계 문서는 "일반 조회보다 한 단계 높은 접근 제한"을 권고만 하고 구체적 방법은 이 시스템에 맡겼다. "관리자 한 명만 수정 가능하게 하고 싶다"는 요청에 따라, **일반 접속 암호와 별개인 2차 암호**를 뒀다 — 이 암호를 아는 사람만 들어올 수 있으므로, 그 대상을 관리자 한 명으로 좁히면 사실상 그 사람만 열람·입력할 수 있다. 별도 계정·권한 시스템이 없는 이 앱 구조상, "특정 한 사람만"을 구현하는 유일한 현실적 방법은 그 사람만 아는 비밀번호다.

### 접근 권한 — 조회는 모두, 수정은 관리자 (2026-09-19 조정)

처음에는 섹션 전체를 2차 암호로 잠갔으나, **"평소에는 누구나 확인 가능하고 수정만 관리자 한 사람"** 으로 요건이 바뀌어 읽기/쓰기를 분리했다.

| | 조회(이력 표·CSV 내보내기) | 입력·삭제 |
|---|---|---|
| 일반 작업자 | ✅ 가능 | ❌ 불가 (버튼 자체가 안 보임) |
| 관리자(2차 암호 입력) | ✅ 가능 | ✅ 가능 |

- 이력 표는 잠금과 무관하게 항상 보이고, **입력 폼과 삭제 버튼만** 2차 암호로 잠근다(`renderCalibList()`의 `canEdit`).
- 계정·권한 체계가 없는 구조라, "특정 한 사람만 수정"은 **그 사람만 아는 암호**로 구현할 수밖에 없다. 암호를 공유하는 순간 그 사람들도 수정할 수 있게 되는 것이 이 방식의 한계다.
- 현재 관리자 비밀번호는 **`dnkm8721*`** — QR 정보관리 시스템의 2차 암호(그 저장소 `gate.js`의 `HASH_2`)와 **같은 값**이다(2026-09-19 통일). 두 시스템에서 "한 단계 더 민감한 자료"를 여는 암호를 하나로 맞춰 담당자가 외울 것을 줄이려는 의도. 바꾸려면 `index.html`의 `CALIB_HASH` 상수를 새 암호의 SHA-256 해시로 교체.
  - 트레이드오프(알고 쓸 것): 1차 접속 암호(`dnkm8721`)에 문자 하나를 붙인 형태라, 1차 암호를 아는 사람이 유추하기 어렵지 않다. 이 시스템 전체가 "실제 보안 장치가 아니라 우연한 열람을 막는 수준"(§11 한계)이라는 전제 위에서 **외우기 쉬움을 택한 결정**이다. 실제로 수정 권한을 엄격히 통제해야 한다면 유추하기 어려운 별도 값으로 바꿔야 한다.
- **`sessionStorage`를 쓴다.** 탭을 닫으면 관리자 모드가 풀린다. 1차 접속 암호도 2026-09-19(5차)부터 `sessionStorage`로 바뀌어 지금은 재확인 주기가 같지만, 수정 권한이라는 성격상 세션 단위 재확인이 맞다고 보고 그대로 둔다 — QR 정보관리 시스템이 2차 암호에 `sessionStorage`를 쓰는 것과 같은 이유.
- 채널을 넘나들어도(예: 130-h → 130-i) 한 번 풀면 그 탭에서는 계속 관리자 모드다. 보정 이력 데이터 자체는 채널마다 별도 저장된다.

### 「기타사항」 잠금 (2026-09-19 추가)

섹션을 통째로 감추고 제목만 **"기타사항"** 으로 바꾸는 버튼. 남이 폰을 봤을 때 이 섹션이 무엇인지 드러나지 않게 하는 용도다.

- 잠그면 제목이 "기타사항"이 되고 내용 대신 암호 입력칸만 남는다. 감춰진 동안에는 표 내용을 **DOM에서도 비운다** — 숨김 처리만 해 두면 이전에 그려진 내용이 그대로 남아 감추는 의미가 옅어지기 때문.
- **모든 사람의 화면에 함께 적용된다.** 상태는 `db.settings.calibHidden` 에 두고 구글시트의 **"설정" 시트**로 공유한다(`pull()` 응답에 항상 실려 옴). 한 사람이 잠그면 다른 폰에서도 잠긴다.
- **잠금 해제 암호는 게이트(접속) 암호와 동일**하다. 작업자 모두가 아는 암호이므로 이건 "외부인 눈에 안 띄게" 하는 수준이지 내부 권한 구분이 아니다 — 내부 권한 구분은 위의 관리자 암호(입력·삭제)가 담당한다.
- **잠그기 버튼은 관리자 모드 안에만 있다(2026-09-19(7차)).** 원래(3차)는 누구나 누를 수 있게 뒀었는데, "관리자 모드로 전환해서 들어가면 입력·수정과 「기타사항」 잠그기가 같이 있게 해 달라"는 요청으로 `calibLockZone`을 만들어 `calibEditor`(관리자 모드에서만 보이는 입력 폼)와 함께 묶었다. 즉 지금은 **잠그기도 관리자 암호가 있어야** 가능하고, 풀기(위 잠금 해제 암호)만 접속 암호로 된다.
- 해시를 두 파일에 복사해 두면 암호를 바꿀 때 한쪽만 바뀌는 사고가 나므로, `gate.js`가 `window.dnkGate.passHash`/`sha256Hex`로 넘겨주고 `index.html`은 그걸 받아 쓴다.
- **이 섹션에는 암호 입력칸이 두 개다**(잠겼을 때만 보이는 해제용, 평소에 보이는 관리자용) — 서로 다른 암호를 받는데도 둘 다 그냥 "암호 입력"이라고만 되어 있어 헷갈린다는 문의가 있었다(2026-09-19). 각 입력칸 밑에 **어느 암호인지**(접속 암호와 같음 / 접속 암호와 다름)를 짧게 안내하는 문구를 추가했다.

### 데이터 구조

| 필드 | 설명 |
|---|---|
| 일자 | 보정 작업 수행일 |
| 샘플 | ④에 등록된 마스터 샘플 중 선택 (샘플ID로 저장, 화면엔 그 시점 샘플명도 같이 저장) |
| 리크값 - 전 / 후 | 보정 작업 전/후 측정된 리크값(mL/min) |
| comp value - 전 / 후 | 보정 작업 전/후 디텍터 Fixed comp value |
| 함침여부 | 마스터 샘플을 재함침 처리했으면 체크 |
| 설비주변온도 | 측정 시점 온도(℃) |
| 판정 | OK/NG (자동 제안, 수동 확인) |
| 확인자 · 비고 | 자유 텍스트 |

**수정(덮어쓰기)은 지원하지 않는다** — 원본 설계가 "새 이력은 맨 아래에 계속 추가"였고, 같은 날 같은 샘플을 두 번 보정하는 경우도 있어 (일자+샘플)을 자연키로 묶을 수 없다. 잘못 입력했으면 **삭제 후 다시 추가**한다(삭제 전 확인창 있음).

### 서버(구글시트) 반영

`tools/apps-script/Code.gs`에 새 시트 **"보정이력"**과 `calibPush` 액션이 추가됐다. **이미 구글시트 연동을 설정해 두었다면 Apps Script 재배포가 필요하다** (`구글시트_연동_설정방법.md` 맨 위 안내 참고). 재배포 전에는 이 섹션에 입력한 값이 그 폰에만 남고 다른 폰에는 안 보인다 — 관리자 혼자 쓰는 섹션이라도, 관리자가 폰을 여러 대 쓰거나 나중에 다른 사람이 인수인계 받는 상황을 생각하면 재배포는 해 두는 것이 좋다.

### 이관 대상 실데이터 (아직 반영 전)

인계 문서에 남아 있던 실제 이력 2건은 공정No.130(현재 채널로는 130-h·130-i)에 각각 하나씩 있다. **자동으로 옮기지 않았다** — 확인자 칸이 원래 비어 있던 값이라 담당자 확인 없이 그대로 옮기는 것이 부적절하다고 판단해서다(인계 문서 자체도 "담당자명은 실제 작업자에게 확인 후 채워 넣어야 한다"고 명시). 아래 값을 이 기능으로 직접 한 번 입력해 두는 것을 권장한다.

| 채널 | 일자 | 샘플 | 리크값(전→후) | comp value(전→후) | 함침 | 온도 | 판정 |
|---|---|---|---|---|---|---|---|
| 130-h | 2026-09-02 | OK 마스터 | -0.14 → 0.64 | 3.5 → 3.0 | Y | 22.4℃ | OK |
| 130-i | 2026-09-02 | OK 마스터 | 2.84 → 1.17 | 0 → 2 | Y | 22.4℃ | OK |

---

## 5. 데이터 저장 — 폰 로컬 + 구글시트 동기화

- 입력값은 **항상 이 폰의 브라우저(localStorage)에 먼저 저장**된 뒤 화면에 반영되고, 그다음 구글시트로 전송된다. 전파가 약해도 입력이 막히지 않는다.
- 전송 실패분은 "미전송 N건"으로 쌓여 자동 재전송된다.
- 여러 폰이 같은 데이터를 보려면 구글시트 연동 설정이 **선행 조건**이다 — 설정 전에는 그 폰에만 저장된다(화면에 "이 폰에만 저장됨" 표시).
- 최초 설정은 구글 계정 소유자가 직접 해야 한다 (로그인·권한 승인 필요) — `구글시트_연동_설정방법.md` 참고, 10~15분.
- 상세 설계 판단(왜 JSONP인지, 왜 폰 저장이 먼저인지 등)은 QR 정보관리 시스템의 `개선이력.md` 2026-09-15·09-16 항목에 기록되어 있었으나, 프로젝트 분리로 그 문서에서는 제거되었다. 필요하면 이 문서 §7에 옮겨 적을 것.

---

## 6. QR 코드

`tools/make_qr.py` 로 생성. **2026-09-18, 채널을 4개 → 6개로 나누면서 재생성했다** (§3-1 참고). 2026-09-16 조직 이전 때도 한 번 재생성했었다 — 다행히 두 번 다 라벨 출력 전이라 실물 재출력 비용은 없었음:

| 파일 | 대상 | 주소 |
|---|---|---|
| `qr-codes/eq-120.png` | 공정No.120 조립전 유로계 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=120` |
| `qr-codes/eq-130-h.png` | 공정No.130 조립전 전체계 — 하우징 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=130-h` |
| `qr-codes/eq-130-i.png` | 공정No.130 조립전 전체계 — 인버터 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=130-i` |
| `qr-codes/eq-180.png` | 공정No.180 조립후 유로계 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=180` |
| `qr-codes/eq-190-h.png` | 공정No.190 조립후 전체계 — 하우징 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=190-h` |
| `qr-codes/eq-190-i.png` | 공정No.190 조립후 전체계 — 인버터 | `https://dnk-mobility.github.io/eawd-leak-management/?eq=190-i` |

옛 `eq-130.png`/`eq-190.png`(하우징·인버터 통합 채널용)는 삭제했다 — 더 이상 어떤 채널도 그 주소를 쓰지 않는다.

저장소 이름이나 조직명이 또 바뀌면 주소도 바뀌므로 `tools/make_qr.py` 의 `BASE` 값을 맞추고 다시 실행해야 한다 (그러면 현장에 붙인 QR 라벨도 재발급 필요 — **그래서 QR을 실제로 출력·부착하기 전에 계정/조직 구조·채널 구성부터 확정해 두는 것이 중요하다**, §9 참고). 130·190은 설비 1대에 QR 2개(하우징용·인버터용)를 붙이게 된다 — 현장 부착 시 헷갈리지 않도록 라벨에 "하우징"/"인버터" 표기를 크게 넣을 것을 권장한다.

---

## 7. 관리자 다운로드 (데이터 · 그래프)

⑥ "데이터 · 연동 설정" 섹션에서 세 가지를 받을 수 있다:

| 버튼 | 내용 | 형식 |
|---|---|---|
| CSV 내보내기 | 그 설비의 전체 기록 (일자·교대·샘플·값·판정 등) | `.csv` (UTF-8 BOM — 엑셀에서 한글 안 깨짐) |
| 전체 백업 파일 | 전 설비 데이터 원본 그대로 | `.json` (다른 폰에 복원 가능) |
| **이번 달 그래프 이미지 저장** | 현재 보고 있는 달의 샘플별 꺾은선 그래프 | `.png` (2026-09-16 추가) |

**그래프 PNG는 외부 라이브러리 없이 만든다.** 화면에 그리는 SVG를 그대로 `<img>`로 로드해 캔버스에 3배 해상도로 그린 뒤 `canvas.toBlob()`으로 PNG를 뽑는다(`svgToPngBlob()`). 라이브러리 추가 없이 브라우저 표준 API만 사용 — 2026-09-15 그래프 자체를 만들 때 정한 "외부 의존성 없음" 원칙을 그대로 따른 것.

이미지 안에도 제목을 직접 그려 넣는다(`buildChart()`의 `titleLine1`/`titleLine2` — 설비명·공정No·연월 / 샘플명·스펙·판정요약). 화면 밖 HTML(카드 헤더)에만 있던 정보를 SVG 안으로 옮긴 이유는, PNG로 따로 저장했을 때 파일만 봐도 어느 설비·샘플·달인지 알 수 있어야 보고 자료로 쓸 수 있기 때문이다. 이 때문에 차트 SVG의 `viewBox` 높이를 168→190, 위쪽 여백(`PT`)을 14→34로 늘렸다.

여러 샘플이 있으면 버튼 한 번에 샘플 수만큼 PNG가 순차 다운로드된다(사이 400ms 텀 — 브라우저가 다중 다운로드를 한꺼번에 막는 경우 대비).

## 8. 데이터 공유 (구글시트 연동) — 2026-09-16 설정 완료

**연동 설정 전에는 각 폰에만 데이터가 저장된다.** "각 사용자가 입력하면 모두에게 반영"되게 하려면 `구글시트_연동_설정방법.md`의 1~5단계(구글 계정 소유자가 직접, 10~15분)를 거쳐야 한다 — 이건 계정 인증이 걸린 절차라 대신 해줄 수 없다.

- 설정 전: 화면 상단 띠에 "이 폰에만 저장됨" 표시. 정상 동작하지만 폰마다 데이터가 따로 논다.
- 설정 후: "구글시트 연동됨" 표시로 바뀌고, 어느 폰에서 입력해도 같은 데이터를 본다. 원본은 구글 스프레드시트에 쌓여 관리자가 시트를 직접 열어봐도 된다(위 ⑥ 다운로드 버튼과는 별개의, 상시 열람 가능한 원본 저장소).

**연동 완료 및 실사용 검증 (2026-09-16).** `leak-sync.js`의 `URL`에 배포된 Apps Script 웹앱 주소 반영, 실제 배포 사이트에서 3대의 "기기"(localStorage를 지워 새 기기처럼 재접속)로 교차 검증:

| 항목 | 결과 |
|---|---|
| 기기 A → 저장 | 공정No.120에 값 저장, `queueSize()` 0건 확인(서버 전송 성공, 대기열에 안 쌓임) |
| 기기 B → 조회 | localStorage 초기화 후 재접속 시 동일 일자·교대 기록이 자동으로 채워짐(일자·온도·확인자·비고 일치) |
| 기록 삭제 → 기기 C → 조회 | 기기 B에서 삭제 → 새 기기(C)로 재접속 시 기록 0건, 서버(Apps Script `pull`)도 직접 조회해 `recs:[]` 확인 |

**한 가지 확인된 동작(버그 아님):** 검증 중 ④ 마스터 샘플·스펙 관리를 한 번도 저장하지 않은 상태로 값을 입력했더니, 새 기기에서 그 기록의 `items[].sid`가 새 기기가 로컬로 새로 생성한 샘플 id와 달라 값 입력란이 비어 보이는 현상이 있었다. 원인은 `applyPulled()`가 `data.samples.length`가 0이면(= 서버에 스펙이 한 번도 저장된 적 없으면) 로컬 기본 샘플을 그대로 두기 때문 — **실사용에서는 관리자가 ④에서 스펙을 최초 1회 저장하면(그 순간 `pushCurrentSamples()`가 서버에 샘플 id를 등록) 그 이후 모든 기기가 같은 id를 pull 받아 정상 매칭된다.** 운영 시작 시 체크리스트: 설비 4개소 모두 ④에서 스펙(지정값/상·하한)을 최소 1회 저장해 둘 것.

## 9. 계정·조직 소유권 (담당자 교체에 대비)

**GitHub·구글시트 계정을 특정 개인 명의로 두지 않는다.** 담당자가 퇴사하면 계정이 통째로 사라지거나 접근이 끊기는 문제를 막기 위한 조치.

- **GitHub**: 2026-09-16, 개인 계정(`dongnam959`) 소유였던 이 저장소와 QR 정보관리 시스템 저장소를 **`dnk-mobility` 조직(Organization)**으로 이전했다. 조직은 공동 관리자를 여러 명 등록할 수 있어, 한 사람이 나가도 다른 관리자가 그대로 운영할 수 있다. (이전 전: `dongnam959/eawd-leak-management` → 이전 후: `dnk-mobility/eawd-leak-management`, 무료 요금제 그대로)
- **구글시트 연동 계정**: 회사에 부서 공용 메일함이 없어(직원별 개인 메일만 존재), **부서명 의미의 신규 계정**(예: "DnK 후공정 생산기술팀")을 새로 만들어 쓴다. 이 계정은 특정 개인 소유가 아니라 팀 자산으로 취급하고, **복구 이메일을 현재 담당자의 회사 개인 메일로 걸어 둔다.** 담당자가 바뀌면 복구 이메일만 후임자 메일로 갱신하고 비밀번호를 인계하면 되며, 계정을 새로 만들 필요가 없다.
- QR 라벨을 실제로 출력·부착하기 전에 이 구조를 먼저 정리해 둔 이유: 계정/저장소 소유권을 나중에 바꾸면 주소가 바뀌어 이미 붙인 라벨을 전부 재출력해야 한다. 지금(2026-09-16 기준 양쪽 다 라벨 미부착) 이 문제를 해결하는 것이 가장 비용이 적게 든다.

## 10. 향후 기록 위치

앞으로 이 프로젝트에 변경사항이 생기면, QR 정보관리 시스템의 `개선이력.md`처럼 이 파일 하단에 이어서 누적 기록한다 (문제 → 조치 → 검증 → 효과 형식 권장).
