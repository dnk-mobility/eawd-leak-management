# e-AWD 모터 하우징 누설값 관리 시스템 — 프로젝트 개요

> 후공정 생산기술팀 · e-AWD 70kW MOTOR HOUSING ASS'Y 라인
> 2026-09-16: 기존 "공정 설비 정보관리 시스템"(dongnam959.github.io, QR코드 설비 정보시스템)에서 완전히 분리해 신설. 두 시스템은 코드·저장소·작업 폴더가 전부 별개다.

---

## 1. 목적

리크테스터 4개소(공정No.120·130·180·190)가 매일 시업 전 OK/NG 마스터 샘플로 검사기 정상 여부를 확인하고 누설값을 기록하는 작업을, 종이 기록지 대신 QR 스캔 → 스마트폰 입력으로 대체한다. 저장과 동시에 합격/불합격 판정과 추이(꺾은선) 그래프가 자동으로 나온다.

기존 QR 정보관리 시스템과는 **완전히 독립된 별도 저장소**다. 분리 이유: QR 정보관리 시스템은 "찍으면 문서를 읽는" 용도라 인트로(4.5초)·2단계 암호 등 진입 절차가 있고, 이 프로젝트는 "찍으면 바로 입력"이 핵심이라 그 절차들이 방해가 된다.

---

## 2. 배포 구조

| 항목 | 내용 |
|---|---|
| 로컬 개발 환경 | `C:\Users\user\Desktop\후공정 리크 설비 마스터 샘플 기록관리 프로젝트` |
| 저장소 | [`dongnam959/eawd-leak-management`](https://github.com/dongnam959/eawd-leak-management) |
| 브랜치 | `main` (단일 브랜치) |
| 배포 방식 | GitHub Pages (project site — QR 정보관리 시스템과 달리 `<계정>.github.io` 루트가 아니라 `<계정>.github.io/<저장소명>/` 하위 경로) |
| 실서비스 주소 | https://dongnam959.github.io/eawd-leak-management/ |
| 빌드 도구 | 없음 — 순수 정적 HTML/CSS/JS |
| 저장소 생성 | 2026-09-16, GitHub CLI(`gh repo create`)로 생성 + Pages API로 활성화 |

---

## 3. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 전체 화면 1장. 설비 전환 탭 4개(`?eq=120\|130\|180\|190`) + 입력·그래프·월별기록·스펙관리·백업 |
| `gate.js` | 접속 암호 — **기기당 1회만** 확인(localStorage). QR 정보관리 시스템의 2단계 세션 방식과 다름(§4) |
| `leak-sync.js` | 구글 스프레드시트 연동 — 폰 저장 우선, 그 다음 전송(§5) |
| `styles.css` | QR 정보관리 시스템에서 그대로 가져온 공용 스타일 (id-bar·section·표 등) |
| `favicon.svg` | 탭 아이콘 (QR 정보관리 시스템과 동일 디자인) |
| `robots.txt` | 검색엔진 수집 차단 |
| `tools/apps-script/Code.gs` | 구글 시트 쪽 서버 코드 원본 (Apps Script 편집기에 배포된 것과 동일본 — 백업/참고용) |
| `구글시트_연동_설정방법.md` | 최초 1회 연동 설정 절차 (계정 소유자가 직접 수행) |
| `qr-codes/eq-120.png` 등 4개 | 설비별 QR — **저장소 주소가 확정되면 재생성 필요** (§6) |

---

## 4. 접속 암호 (`gate.js`)

- 예전 QR 시스템은 `sessionStorage`(탭 단위) — QR을 다시 찍어 새 탭이 열릴 때마다 재인증.
- 이 프로젝트는 `localStorage`(기기 단위) — **그 폰에서 최초 1회만** 맞히면 이후 계속 통과.
- 설비별 전용 폰/태블릿 1대를 쓰는 운영과 맞는 선택. 여러 사람이 같은 폰을 교대로 써도 다시 안 물어봄.
- 2차 암호 단계 없음 (기술문서·PDF 뷰어가 없는 프로젝트라 불필요).
- 현재 암호: 기존 QR 정보관리 시스템과 **동일** (`PASS_HASH`는 `gate.js` 안). 바꾸려면 새 암호의 SHA-256 해시로 교체.

---

## 5. 데이터 저장 — 폰 로컬 + 구글시트 동기화

- 입력값은 **항상 이 폰의 브라우저(localStorage)에 먼저 저장**된 뒤 화면에 반영되고, 그다음 구글시트로 전송된다. 전파가 약해도 입력이 막히지 않는다.
- 전송 실패분은 "미전송 N건"으로 쌓여 자동 재전송된다.
- 여러 폰이 같은 데이터를 보려면 구글시트 연동 설정이 **선행 조건**이다 — 설정 전에는 그 폰에만 저장된다(화면에 "이 폰에만 저장됨" 표시).
- 최초 설정은 구글 계정 소유자가 직접 해야 한다 (로그인·권한 승인 필요) — `구글시트_연동_설정방법.md` 참고, 10~15분.
- 상세 설계 판단(왜 JSONP인지, 왜 폰 저장이 먼저인지 등)은 QR 정보관리 시스템의 `개선이력.md` 2026-09-15·09-16 항목에 기록되어 있었으나, 프로젝트 분리로 그 문서에서는 제거되었다. 필요하면 이 문서 §7에 옮겨 적을 것.

---

## 6. QR 코드

`tools/make_qr.py` 로 생성. 저장소 주소가 확정된 뒤(2026-09-16) 아래 최종 주소로 재생성 완료:

| 파일 | 대상 | 주소 |
|---|---|---|
| `qr-codes/eq-120.png` | 공정No.120 (조립전 유로계) | `https://dongnam959.github.io/eawd-leak-management/?eq=120` |
| `qr-codes/eq-130.png` | 공정No.130 (조립전 전체계) | `https://dongnam959.github.io/eawd-leak-management/?eq=130` |
| `qr-codes/eq-180.png` | 공정No.180 (조립후 유로계) | `https://dongnam959.github.io/eawd-leak-management/?eq=180` |
| `qr-codes/eq-190.png` | 공정No.190 (조립후 전체계) | `https://dongnam959.github.io/eawd-leak-management/?eq=190` |

저장소 이름을 바꾸면 주소가 바뀌므로 `tools/make_qr.py` 의 `BASE` 값을 맞추고 다시 실행해야 한다 (그러면 현장에 붙인 QR 라벨도 재발급 필요).

---

## 7. 향후 기록 위치

앞으로 이 프로젝트에 변경사항이 생기면, QR 정보관리 시스템의 `개선이력.md`처럼 이 파일 하단에 이어서 누적 기록한다 (문제 → 조치 → 검증 → 효과 형식 권장).
